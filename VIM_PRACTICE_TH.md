# VIM_PRACTICE — สนามฝึกลงมือจริง (hands-on playground)

คู่มืออื่นๆ (`VIM_TUTORIAL.md`, `VIM_TEXT_OBJECTS.md`, `NVIM_GUIDE_TH.md`) บอก **ทฤษฎี + ตารางปุ่ม** แล้ว
ไฟล์นี้ต่างออกไป: มัน **ให้ข้อความจริงมาแก้ในไฟล์นี้เลย** แต่ละ drill มี "ก่อน" ให้ลงมือ และ "หลัง" ให้เช็คว่าทำถูกไหม

> 🇹🇭 **ทำไมข้อความฝึกเป็นภาษาอังกฤษ:** ตั้งใจครับ — motion แบบ word (`w`/`b`/`e`) และ text object จะทำงานตรงไปตรงมาบนอังกฤษ/โค้ด และเลี่ยงปัญหา render สระ/วรรณยุกต์ไทยในกริด (ดู `NVIM_GUIDE_TH.md` §0.5 ทำไม). คำอธิบายเป็นไทย แต่ของที่แก้เป็นอังกฤษ

---

## วิธีใช้ไฟล์นี้ (อ่านก่อน)

1. เปิดไฟล์นี้: `nvim ~/.config/nvim/VIM_PRACTICE_TH.md` (บน Windows: `nvim $env:LOCALAPPDATA\nvim\VIM_PRACTICE_TH.md`) หรือกด `<C-p>` แล้วพิมพ์ `practice`
2. เลื่อนไปที่ drill ที่อยากฝึก → ทำบน **บรรทัดในกรอบ "ก่อน"** → เทียบกับ "หลัง"
3. **ทำพลาด/อยากลองใหม่** เลือกได้ 3 ทาง:
   - `u` = undo ทีละขั้น (เร็วสุดสำหรับ drill เดียว)
   - `:e!` = โหลดไฟล์ใหม่จากดิสก์ ทิ้งที่แก้ทั้งหมด (**ใช้ได้ถ้ายังไม่ `:w`**)
   - เผลอ `:w` ไปแล้ว → คืนไฟล์เดิมด้วย git (ได้หลังไฟล์นี้ถูก commit แล้ว): `:!git checkout -- VIM_PRACTICE_TH.md` แล้ว `:e!`
4. **กติกา:** ระหว่างฝึก **อย่า `:w`** ถ้าอยากให้ไฟล์กลับมาสะอาดง่ายๆ — พังยังไงก็ `:e!` รีเซ็ตได้

> ⚠️ **หมายเหตุ:** drill หมวด 1-10 ใช้ text object/motion **built-in** ล้วน · ส่วน plugin ที่เพิ่งเพิ่ม (`vaf`/`vif` function object, flash `s`, surround `gs`) มี drill แยกใน **หมวด 11** ท้ายไฟล์

**ไล่ระดับ 10 หมวด:** motion → operator+count → text object → search/substitute → dot `.` → macro → register → mark → visual block → combo

---

## หมวด 1 — Motions (แค่ขยับเคอร์เซอร์ ยังไม่แก้อะไร)

Motion ไม่เปลี่ยนข้อความ — ให้ดูว่าเคอร์เซอร์ไป "หยุด" ถูกที่ไหม

### 1.1 `w` เทียบ `W`
วางเคอร์เซอร์ต้นบรรทัด กด `w` ซ้ำๆ (หยุดที่ `-` และ `.` ด้วย) แล้ว `0` กลับต้น กด `W` ซ้ำ (ข้ามทั้งก้อนจนเจอช่องว่าง)  ⟹ `w` หยุดถี่กว่า `W`
```text
foo-bar.baz qux zap
```

### 1.2 `f` / `t` / `;` / `,` — พุ่งหาตัวอักษรในบรรทัด
ต้นบรรทัด: `f/` ไปหยุด **บน** `/` ตัวแรก · `;` ไป `/` ถัดไป · `,` ย้อนกลับ · `t/` หยุด **ก่อน** `/`
```text
path/to/some/file.txt
```

### 1.3 `0` / `^` / `$`
`^` ไปตัวอักษรแรกที่ไม่ใช่ช่องว่าง · `0` ไปคอลัมน์ 0 จริงๆ (หน้าช่องว่าง) · `$` ไปท้ายบรรทัด
```text
    indented line with spaces in front
```

### 1.4 `%` — กระโดดวงเล็บคู่
วางเคอร์เซอร์บน `(` แล้วกด `%` → เด้งไป `)` ที่คู่กัน · ลองวางบน `[` หรือ `{` แล้ว `%` ดู
```text
foo(bar[baz]{qux})
```

---

## หมวด 2 — Operators + count (สูตร `[count][operator][motion]`)

`d`=ลบ `c`=เปลี่ยน(ลบ+เข้า insert) `y`=ก๊อป — ทุกตัวใช้สูตรเดียวกัน ตรงนี้ฝึกด้วย `d`/`>` แต่ `c`/`y` ก็เหมือนกัน

### 2.1 `dw` — ลบทีละคำ
วางเคอร์เซอร์บน `delete` → `dw`  ⟹ `this word stays`
```text
delete this word stays
```

### 2.2 `d2w` — count + operator
วางเคอร์เซอร์บน `one` → `d2w` (ลบ 2 คำ)  ⟹ `three four`
```text
one two three four
```

### 2.3 `>>` — เยื้อง 1 บรรทัด (1 ระดับ = `shiftwidth`)
วางเคอร์เซอร์บรรทัดนี้ → `>>` เยื้อง 1 ระดับ (ไฟล์ `.md` นี้ = **4 ช่อง**; ไฟล์โค้ด `.lua`/`.py` = **2 ช่อง** ตามคอนฟิก) · `<<` ถอยกลับ
```text
badly indented line
```

### 2.4 `>ip` — operator + text object (เยื้องทั้งย่อหน้า)
วางเคอร์เซอร์ตรงไหนก็ได้ใน 3 บรรทัดล่าง → `>ip`  ⟹ ทั้ง 3 บรรทัดเยื้อง 1 ระดับ (ในไฟล์ `.md` นี้ = 4 ช่อง)

> บล็อกนี้ **ไม่ใส่กรอบโค้ด** เพราะ vim นับ "ย่อหน้า" (`ip`/`ap`) จาก **บรรทัดว่าง** ไม่ใช่กรอบ markdown — ถ้าใส่กรอบ `>ip` จะเผลอกินบรรทัดรั้วโค้ด (code fence) ไปด้วย (บั๊กที่ codex จับได้!)

first line of the paragraph
second line of the paragraph
third line of the paragraph

---

## หมวด 3 — Text Objects (`[operator][i/a][object]`)

`i`=inner (ข้างในล้วน) · `a`=around (รวมขอบ) · เคอร์เซอร์แค่ต้องอยู่ "ข้างใน" ก็พอ ไม่ต้องเล็งขอบ

### 3.1 `ciw` — เปลี่ยนทั้งคำ
เคอร์เซอร์ที่ไหนก็ได้บน `helllo` → `ciw` พิมพ์ `hello` → `<Esc>`  ⟹ `hello world`
```text
helllo world
```

### 3.2 `ci"` — เปลี่ยนข้างในเครื่องหมายคำพูด
เคอร์เซอร์ข้างในเครื่องหมาย `"` → `ci"` พิมพ์ `hello` → `<Esc>`  ⟹ `msg = "hello"`
```text
msg = "helo"
```

### 3.3 `ci(` — เปลี่ยน argument ในวงเล็บ
เคอร์เซอร์ข้างในวงเล็บ → `ci(` พิมพ์ `10, 20` → `<Esc>`  ⟹ `sum(10, 20)`
```text
sum(1,2)
```

### 3.4 `cit` — เปลี่ยนข้างใน HTML tag
เคอร์เซอร์ข้างในแท็ก → `cit` พิมพ์ `Title` → `<Esc>`  ⟹ `<h1>Title</h1>`
```text
<h1>Ttile</h1>
```

### 3.5 `dap` — ลบทั้งย่อหน้า (รวมบรรทัดว่างท้าย)
เคอร์เซอร์ในย่อหน้าบน (`delete this whole paragraph`) → `dap`  ⟹ ลบย่อหน้าบน + บรรทัดว่างท้าย เหลือแค่ `keep this paragraph`

> เช่นเดียวกับ 2.4: บล็อกนี้ **ไม่ใส่กรอบโค้ด** — `ip`/`ap` คั่นย่อหน้าด้วยบรรทัดว่าง ถ้าใส่กรอบจะเผลอกินบรรทัดรั้วโค้ดไปด้วย

delete this whole paragraph
these two lines vanish together

keep this paragraph

### 3.6 `d2i(` — text object ซ้อนชั้น (count = ชั้นที่เท่าไร)
เคอร์เซอร์บน `x` → `di(` ได้ `outer(inner())` · รีเซ็ต (`u`) แล้วลอง `d2i(`  ⟹ `outer()`
```text
outer(inner(x))
```

> ✅ อัปเดต: `vaf`/`vif` (ทั้งฟังก์ชัน) **ใช้ได้แล้ว** (เพิ่ม nvim-treesitter-textobjects) — ฝึกใน **หมวด 11** (ต้องเปิดไฟล์โค้ดจริง ไม่ใช่ในไฟล์ `.md` นี้)

---

## หมวด 4 — Search & Substitute

> ℹ️ หมวดนี้แก้ทั้ง block — เสร็จแล้วรีเซ็ตด้วย `u` หรือ `:e!` ก่อนไป drill ถัดไป

### 4.1 `/` `n` `N` `*` — ค้นแล้วกระโดด
กด `/TODO` แล้ว `<CR>` → `n` ไปตัวถัดไป, `N` ย้อนกลับ · หรือวางเคอร์เซอร์บนคำ `TODO` แล้วกด `*` (ค้นคำใต้เคอร์เซอร์)
```text
TODO refactor this
done: cleanup done
TODO write the tests
TODO review the PR
```

### 4.2 `:%s/.../.../g` — แทนที่ทั้งไฟล์
วางเคอร์เซอร์ที่ไหนก็ได้ พิมพ์ `:%s/zeta/omega/g` แล้ว `<CR>`
⚠️ `%` = **ทุกบรรทัดในไฟล์** → `zeta` เปลี่ยนหมด รวมทั้งในบล็อกล่าง **และในคำสั่ง/คำอธิบายบรรทัดนี้เองด้วย!** (นั่นแหละความหมายของ `%`) เสร็จแล้ว `:e!` รีเซ็ต · เวอร์ชันถามยืนยันทีละตัว: `:%s/zeta/omega/gc` (`y`=ใช่ `n`=ข้าม `a`=ทั้งหมด `q`=เลิก)
```text
zeta = 1
result = zeta + zeta
log(zeta)
```
> อยากจำกัดเฉพาะบางบรรทัด: เลือกบล็อกด้วย visual (`V` แล้วเลื่อน) → `:s/zeta/omega/g` (ขึ้นเป็น `:'<,'>s/...` = เฉพาะที่เลือก) · `:s` เฉยๆ (ไม่มีช่วง) = เฉพาะบรรทัดปัจจุบัน

---

## หมวด 5 — Dot `.` (ปุ่มทรงพลังที่สุด: ทำซ้ำการแก้ล่าสุด)

เคล็ด: ออกแบบการแก้ให้ "ทำซ้ำได้" แล้วใช้ `.` รัวๆ — จับคู่กับ search `n` ได้สวยมาก

### 5.1 เปลี่ยน `red` → `blue` ทุกที่ด้วย `ciw` + `n` + `.`
1. วางเคอร์เซอร์ที่ **ต้นบรรทัด `color: red`** (กด `0`) — สำคัญ! ถ้าเริ่ม *บน* คำ `red` เลย `/red` จะข้ามตัวแรกไป
2. `/red` แล้ว `<CR>` (ตั้ง pattern + เด้งไป `red` ตัวแรกในบล็อก)
3. `ciw` พิมพ์ `blue` → `<Esc>` (แก้ตัวแรก — นี่คือ "การแก้" ที่ `.` จะจำ)
4. `n` (ไป `red` ถัดไป) แล้ว `.` (ทำซ้ำ ciw→blue) — ทำอีก **3 รอบ** (border → shadow → accent)
```text
color: red
border: red
shadow: red
accent: red
```
⟹ ทุกบรรทัดเป็น `blue`. เทียบกับพิมพ์เองทีละตัว — `.` ประหยัดมหาศาล

> ทำแค่ 4 บรรทัดในบล็อกพอ — กด `n` เกินจะวิ่งไปเจอคำเดิมในหัวข้อ/คำอธิบายด้านบน (แล้ว `.` เผลอแก้ตรงนั้น) เผลอก็ `:e!` รีเซ็ต

---

## หมวด 6 — Macros (`q` อัด, `@` เล่นซ้ำ)

Macro = อัดชุดคีย์ทั้งชุดไว้ใน register แล้วยิงซ้ำ เหมาะกับงานซ้ำๆ ที่ dot ตัวเดียวไม่พอ

### 6.1 ทำ list ให้เป็น `- item,` ทุกบรรทัด
วางเคอร์เซอร์บรรทัด `apple`:
1. `qa` (เริ่มอัดลง register `a`)
2. `I- ` → `<Esc>` (เติม `- ` หน้าบรรทัด)
3. `A,` → `<Esc>` (เติม `,` ท้ายบรรทัด)
4. `j` (ลงบรรทัดถัดไป — **สำคัญ:** ใส่ท้าย macro เพื่อให้เล่นซ้ำได้)
5. `q` (หยุดอัด)
6. `2@a` (เล่นซ้ำอีก 2 รอบ กับ banana + cherry) · หรือ `@a` แล้ว `@@`
```text
apple
banana
cherry
```
⟹
```text
- apple,
- banana,
- cherry,
```
> ⚠️ ทำไมไม่ห่อด้วย `"..."` แบบตำราทั่วไป: คอนฟิกนี้มี **mini.pairs** ปิด quote/วงเล็บอัตโนมัติ → พิมพ์ `"` เดี่ยวๆ จะได้ `""` (คู่) ทันที macro เลยเพี้ยน · เราจึงใช้ `- ` กับ `,` ที่ไม่โดน auto-pair · พัง? `u` ถอยแล้วอัดใหม่

---

## หมวด 7 — Registers (ที่เก็บหลายช่อง ไม่ใช่ clipboard ช่องเดียว)

### 7.1 กับดัก: ลบแล้ว paste ได้ผิด → แก้ด้วย `"0p`
`yy`/`yiw` เก็บของลง register `0` (ช่อง "ของที่ yank ล่าสุด") ด้วยเสมอ — ต่อให้ `dd` ลบอย่างอื่นทับ register ไม่มีชื่อ ของใน `0` ยังอยู่
1. วางเคอร์เซอร์บรรทัด `yankme` → `yy` (ก๊อปบรรทัด — เข้า register `0` ด้วย)
2. `j` ไป `dropme` → `dd` (ลบทิ้ง — register ไม่มีชื่อ = dropme แล้ว)
3. `"0p` (วางจาก register `0` = **yankme** ที่ยังอยู่ ไม่ใช่ dropme)
```text
yankme
dropme
anchor
```
⟹
```text
yankme
anchor
yankme
```
> ถ้ากด `p` เฉยๆ ในสเต็ป 3 จะได้ `dropme` (ผิด) — `"0p` คือพระเอก (ดึงของที่ yank ล่าสุด ไม่ปนกับของที่ลบ)

### 7.2 register มีชื่อ `"a`
`"ayy` เก็บบรรทัดนี้ลงช่อง `a` (เหมือนพักไว้ในกระเป๋า) → ไปลบ/แก้อะไรก็ได้ → `"ap` ดึงออกมาวาง ยังอยู่ครบ · ดูทุกช่องด้วย `:reg`
```text
stash me into register a
```

---

## หมวด 8 — Marks (ปักหมุดตำแหน่ง แล้วกระโดดกลับ)

Mark ไม่แก้ข้อความ — ฝึกกระโดดไป-กลับให้แม่น
1. วางเคอร์เซอร์บรรทัด `SET MARK HERE` → `ma` (ปักหมุดชื่อ `a`)
2. `G` ลงไปบรรทัดล่างสุดของไฟล์ (หรือ `}` ข้ามย่อหน้า)
3. `` `a `` (backtick + a) → เด้งกลับมา **ตำแหน่งเป๊ะ** ที่ปักไว้ · `'a` (quote + a) → กลับมาแต่ที่ต้นบรรทัด
```text
SET MARK HERE (กด ma ตรงนี้)
filler line 1
filler line 2
filler line 3
jump back here with backtick-a
```
> `` `` `` (backtick สองครั้ง) = สลับไป-กลับตำแหน่งก่อนกระโดดล่าสุด · `:marks` ดูหมุดทั้งหมด

---

## หมวด 9 — Visual Block `<C-v>` (แก้หลายบรรทัดพร้อมกันเป็นคอลัมน์)

### 9.1 ใส่ comment `# ` หน้าทุกบรรทัด
1. วางเคอร์เซอร์ที่ตัวอักษรแรกของ `alpha` (คอลัมน์ 0)
2. `<C-v>` (เข้า visual block) → `3j` (เลือกลง 4 บรรทัด เป็นแท่งแนวตั้ง)
3. `I# ` (พิมพ์ `#` เว้นวรรค) → `<Esc>`  ⟹ **ทุกบรรทัด** ได้ `# ` นำหน้า (ผลโผล่ตอนกด `<Esc>`)
```text
alpha
beta
gamma
delta
```
⟹
```text
# alpha
# beta
# gamma
# delta
```
> โบนัส: เติมท้ายบรรทัดด้วย `A` — `<C-v>` `3j` `$` `A;` `<Esc>` = เติม `;` ท้ายทุกบรรทัด (แม้ยาวไม่เท่ากัน เพราะ `$` วิ่งถึงท้ายของแต่ละบรรทัด)

---

## หมวด 10 — Combos เด็ด (ไว้อวด)

### 10.1 `xp` — สลับตัวอักษรที่พิมพ์สลับ
วางเคอร์เซอร์บน `e` ของ `teh` → `xp`  ⟹ `the`
```text
teh cat sat
```

### 10.2 `ddp` — สลับสองบรรทัด
วางเคอร์เซอร์บรรทัด `second` → `ddp`  ⟹ `first` ขึ้นก่อน
```text
second
first
```

### 10.3 `ct<char>` — เปลี่ยนจนถึงตัวอักษร (ไม่รวมตัวนั้น)
วางเคอร์เซอร์บน `o` ของ `oldkey` → `ct:` พิมพ์ `newkey` → `<Esc>`  ⟹ `newkey: value`
```text
oldkey: value
```

---

## หมวด 11 — Plugin ใหม่ (flash · function text object · surround)

3 ตัวนี้เพิ่งเพิ่ม (commit `54850b9`) — `flash` กับ `function text object` ต้อง **ลองในของจริง** (ไฟล์โค้ด/บนจอ) ส่วน `surround` ฝึกในกรอบข้างล่างได้เลย

### 11.1 flash — กระโดดด้วย label (ลองในไฟล์จริง)
เปิดไฟล์โค้ดยาวๆ (เช่น `nvim lua/plugins/init.lua`) → กด `s` แล้วพิมพ์ **ตัวอักษรที่จุดหมาย** (พิมพ์ไปจน label ที่ต้องการโผล่) → กด label เดียว = เด้งไปเลย · `f`/`t` ข้ามบรรทัดได้แล้ว (`;` วนต่อ)
> เป็น motion แบบ interactive — ไม่มี before/after ให้เช็ค ลองเองเข้าใจเร็วสุด

### 11.2 function/class text object (ต้องเปิดไฟล์ `.lua`/`.py` จริง)
⚠️ ในไฟล์ `.md` นี้ **ใช้ไม่ได้** เพราะไม่มีฟังก์ชันให้ treesitter จับ — เปิดไฟล์โค้ดจริงก่อน (เช่น `nvim lua/core/keymaps.lua`) แล้วเลื่อนเคอร์เซอร์เข้าไปในฟังก์ชัน แล้วลอง:
- `vif` เลือกไส้ฟังก์ชัน · `vaf` เลือกทั้งฟังก์ชัน (ดู highlight)
- `dif` ลบไส้ · `daf` ลบทั้งฟังก์ชัน (แล้ว `u` คืน)
- `]f` / `[f` กระโดดฟังก์ชัน ถัดไป/ก่อนหน้า
- `vic` / `vac` = class (ไฟล์ที่มี class เช่น `.py`)

### 11.3 surround — เพิ่ม/ลบ/เปลี่ยนตัวคร่อม (ฝึกในกรอบได้)
เคอร์เซอร์บน `wrap` → `gsaiw"` (ห่อคำด้วย quote)  ⟹ `"wrap" this word` · แล้วเคอร์เซอร์ในเครื่องหมาย → `gsd"` ⟹ กลับเป็น `wrap this word`
```text
wrap this word
```
เคอร์เซอร์ในเครื่องหมาย → `gsr"'` (เปลี่ยน `"` เป็น `'`)  ⟹ `'hi'`
```text
"hi"
```
> `gsaiw)` (ตัวปิด) = `(word)` ไม่เว้นวรรค · `gsaiw(` (ตัวเปิด) = `( word )` · ห่อ HTML tag = `gsaiwt` แล้วพิมพ์ชื่อ tag

---

## แผนฝึก (จาก `NVIM_GUIDE_TH.md` §7.7 — ทำจริงในไฟล์นี้)

อย่าโหมทีเดียวหมด — เลือกสัปดาห์ละ 1-2 หมวด บังคับตัวเองให้ใช้จนคล่อง แล้วค่อยขยับ

- [ ] **สัปดาห์ 1:** หมวด 1 (motion) + 2 (operator+count) — ฐานของทุกอย่าง
- [ ] **สัปดาห์ 2:** หมวด 3 (text object) + 5 (dot `.`) — เร็วขึ้นชัดเจน
- [ ] **สัปดาห์ 3:** หมวด 4 (search/substitute) + 6 (macro)
- [ ] **สัปดาห์ 4:** หมวด 7 (register) + 8 (mark) + 9 (visual block) + 10 (combo)
- [ ] **สัปดาห์ 5:** หมวด 11 (plugin: flash `s` + function text object `vif`/`vaf` + surround `gs`)
- [ ] ทุกวัน: เล่น `:VimBeGood` 5-10 นาที + เลิกใช้ arrow keys (บังคับ `hjkl`)

**ทฤษฎีเต็ม + ตารางปุ่ม:** `NVIM_GUIDE_TH.md` (§2 พื้นฐาน · §3 text object · §7 expert + cheatsheet) · `VIM_TEXT_OBJECTS.md` · `VIM_TUTORIAL.md`

> รีเซ็ตไฟล์ให้สะอาด: `:e!` (ถ้ายังไม่เซฟ) · ถ้าเผลอเซฟ และ commit ไฟล์นี้ไว้แล้ว: `:!git checkout -- VIM_PRACTICE_TH.md` แล้ว `:e!` · สู้ๆ นะครับ ทุกคนเคยผ่านช่วงงงๆ มาก่อน 💪
