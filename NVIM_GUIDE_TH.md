# คู่มือใช้งาน Neovim Config (ฉบับละเอียด) — ภาษาไทย

> ติดตั้งบน Mac: ดู [INSTALL_MACOS_TH.md](INSTALL_MACOS_TH.md) — คู่มือติดตั้งภาษาไทยสำหรับ macOS

> สอนใช้ config Neovim ตัวนี้แบบละเอียด ตั้งแต่พื้นฐานจนถึงระดับ expert พร้อมตัวอย่างจริงทุกหัวข้อ
> **leader = `<Space>`** · เปิด Neovim ไว้ข้างๆ แล้วลองกดตามไปด้วยกันนะครับ
>
> _สร้างโดย Claude + ตรวจความถูกต้องและภาษาไทยด้วย codex (cross-model)._

## สารบัญ

- 1. ภาพรวม & โหมดของ Neovim
- 2. พื้นฐาน Vim ที่ต้องแม่น (รากฐานสู่ expert)
- 3. Text Objects — หัวใจของการแก้โค้ดเร็ว
- 4. Plugins: ค้นหาและนำทางไฟล์
- 5. Plugins: เขียนโค้ด (LSP, Completion, Treesitter)
- 6. Plugins: Git, Terminal และ Workflow จริง
- 7. เส้นทางสู่ Vim Expert + Cheatsheet
- 8. แก้ปัญหาที่พบบ่อย + เคล็ดลับเฉพาะเครื่อง
- 9. ใช้ AI CLI ใน Neovim (Claude Code + codex)

---

## 1. ภาพรวม & โหมดของ Neovim

ยินดีต้อนรับสู่ config ตัวนี้ครับ ก่อนอื่นอยากให้ใจเย็นๆ แล้วค่อยๆ ไปด้วยกัน เพราะ Neovim ไม่ได้ยากอย่างที่หลายคนกลัว มันแค่ "คิดคนละแบบ" กับ editor ที่เราคุ้นเคยเท่านั้นเอง

### 1.1 config ตัวนี้คืออะไร

สิ่งที่คุณกำลังใช้อยู่คือ **Neovim 0.12+** ที่ถูกจัดชุดมาให้เป็นสาย "โมเดิร์นปี 2026" เต็มตัว config ชุดนี้เป็นชุดเดียวกันหมดทั้ง Windows, macOS และ Linux (ขั้นต่ำจริง ๆ คือ 0.11 เพราะใช้ `vim.lsp.config`/`vim.lsp.enable` แต่แนะนำ 0.12 เพราะ nvim-treesitter branch `main` ต้องใช้) หมายความว่าเราไม่ได้ใช้ปลั๊กอินเก่าๆ ที่คนเลิกใช้กันแล้ว แต่เลือกของที่เป็นมาตรฐานใหม่ล่าสุด เช่น:

- ใช้ **lazy.nvim** เป็นตัวจัดการปลั๊กอิน (ตัวที่วงการ Neovim ยอมรับเป็นมาตรฐานตอนนี้) และตั้งค่าให้ปลั๊กอินส่วนใหญ่ **lazy-load** คือโหลดตอนต้องใช้จริงเท่านั้น ทำให้เปิด Neovim ได้เร็วมาก
- ใช้ **LSP แบบ native** ของ Neovim เอง (ผ่าน `vim.lsp`) ไม่พึ่งตัวห่ออย่าง lsp-zero อีกต่อไป — นี่คือทิศทางที่ Neovim อยากให้เราไปในระยะยาว
- ใช้ **blink.cmp** สำหรับ autocomplete และ **nvim-treesitter (main branch)** สำหรับ syntax highlighting ซึ่งเป็นเวอร์ชันใหม่ล่าสุดของทั้งคู่

พูดง่ายๆ คือ config นี้พาคุณเดินตามเส้นทางที่ถูกต้องตั้งแต่แรก ไม่ต้องมารื้อทีหลัง

**leader key ของเราคือ `<Space>` (ปุ่ม Space bar)** — จำคำนี้ให้ขึ้นใจ เพราะคำสั่งเด่นๆ เกือบทั้งหมดในคู่มือนี้จะเริ่มด้วยการกด Space ก่อนเสมอ

> **หมายเหตุเรื่องการเขียน:** ในคู่มือนี้เวลาเห็น `<Space>e` หมายถึง "กด Space แล้วตามด้วย e" (กดทีละปุ่ม ไม่ใช่กดพร้อมกัน) ส่วน `<C-p>` หมายถึง "กด Ctrl ค้างแล้วกด p" และ `<A-w>` คือ "กด Alt ค้างแล้วกด w"

### 1.2 แนวคิดหัวใจ: Modal Editing

นี่คือเรื่องเดียวที่ถ้าเข้าใจแล้ว ทุกอย่างที่เหลือจะง่ายขึ้นทันที

editor ทั่วไป (เช่น VS Code, Notepad) มี "โหมดเดียว" คือเปิดมาก็พิมพ์ได้เลย สิ่งที่พิมพ์จากคีย์บอร์ดจะเข้าไปเป็นข้อความในไฟล์โดยตรง ปุ่มพวกนี้เลยต้องเสียไปกับการพิมพ์อย่างเดียว จะสั่งงานอะไรทีก็ต้องเอื้อมไปกด Ctrl/เมาส์

Neovim คิดต่างออกไป มันเป็น **modal editor** คือมีหลาย "โหมด" และปุ่มเดียวกันทำงานไม่เหมือนกันในแต่ละโหมด หัวใจอยู่ที่ **Normal mode** ซึ่งเป็นโหมดที่คุณจะอยู่เป็นค่าเริ่มต้น ในโหมดนี้ปุ่มตัวอักษรไม่ได้ไว้พิมพ์ แต่ไว้ **สั่งงาน** เช่น:

```
d  = delete (ลบ)
c  = change (เปลี่ยน)
y  = yank (คัดลอก)
w  = word (เลื่อนไปคำถัดไป)
```

พอปุ่มกลายเป็นคำสั่ง เราเลยเอามาต่อกันเป็น "ประโยค" ได้ เช่น `dw` = ลบจากเคอร์เซอร์ไปถึงต้นคำถัดไป, `diw` = ลบคำปัจจุบันทั้งคำ ไม่ว่าเคอร์เซอร์อยู่ตรงไหน, `d2w` = ลบ 2 คำ นี่คือพลังที่ทำให้คนแก้โค้ดใน Neovim ได้เร็ว พอคล่องแล้วจะรู้สึกว่าคิดอะไร นิ้วก็ทำตามได้ทันที — และก็เป็นเหตุผลว่าทำไมช่วงแรกมันถึงงงๆ เพราะสมองยังไม่ชินว่า "ตอนนี้ฉันอยู่โหมดไหน"

**เคล็ดลับสำคัญ:** ถ้าหลงทางเมื่อไหร่ ไม่รู้ว่าตัวเองอยู่โหมดอะไร ให้กด `<Esc>` ไว้ก่อนเสมอ มันจะพาคุณกลับสู่ Normal mode ซึ่งเป็นบ้านที่ปลอดภัย (ยกเว้นอยู่ใน terminal ที่ต้องใช้ `<C-\><C-n>` แทน)

### 1.3 โหมดต่างๆ ที่ต้องรู้จัก

| โหมด | ไว้ทำอะไร | เข้าโหมดยังไง | ออกยังไง |
|------|-----------|---------------|----------|
| **Normal** | นำทาง + สั่งงาน (บ้านหลัก) | `<Esc>` จาก Insert/Visual/Command (จาก terminal ใช้ `<C-\><C-n>`) | — (นี่คือโหมดตั้งต้น) |
| **Insert** | พิมพ์ข้อความจริงๆ | `i` `a` `o` (และอื่นๆ) | `<Esc>` |
| **Visual** | เลือกข้อความเป็นช่วง | `v` `V` `<C-v>` | `<Esc>` |
| **Command** | พิมพ์คำสั่ง `:` | `:` | `<Esc>` หรือ `<CR>` เพื่อรัน |
| **Terminal** | คุมเทอร์มินัลในตัว | เปิดผ่าน `<A-w>`/`<A-q>` | `<C-\><C-n>` |

**Normal mode** — โหมดตั้งต้น เอาไว้เดินเคอร์เซอร์ (`h` ซ้าย, `j` ลง, `k` ขึ้น, `l` ขวา), ลบ, คัดลอก, และเรียกคำสั่งต่างๆ คุณจะใช้เวลาส่วนใหญ่อยู่ที่นี่ ไม่ใช่ใน Insert mode อย่างที่หลายคนเข้าใจผิดตอนแรก

**Insert mode** — โหมดพิมพ์ปกติเหมือน editor ทั่วไป มีหลายวิธีเข้า และแต่ละวิธีมีความหมายต่างกัน ลองสังเกตดีๆ:

```
i  = insert ก่อนเคอร์เซอร์ (พิมพ์แทรกตรงตำแหน่งปัจจุบัน)
a  = append หลังเคอร์เซอร์ (พิมพ์ต่อจากตัวอักษรที่เคอร์เซอร์อยู่)
o  = open บรรทัดใหม่ด้านล่าง แล้วเข้า insert เลย
```

ตัวอย่างจริง: วางเคอร์เซอร์ที่ต้นคำว่า `foo` แล้วกด `a` -> เคอร์เซอร์กระโดดไปหลัง `f` และพร้อมให้พิมพ์ ส่วน `o` เหมาะเวลาอยากขึ้นบรรทัดใหม่เพื่อเขียนโค้ดบรรทัดถัดไป กดครั้งเดียวได้ทั้งเปิดบรรทัดและเข้าโหมดพิมพ์

**Visual mode** — โหมดเลือกข้อความ (เหมือนลากเมาส์ไฮไลต์ แต่ใช้คีย์บอร์ด) มี 3 แบบ:

```
v      = Visual character  เลือกทีละตัวอักษร
V      = Visual line       เลือกทีละบรรทัด (ตัว V ใหญ่)
<C-v>  = Visual block      เลือกเป็นบล็อกสี่เหลี่ยม (Ctrl+v)
```

ตัวอย่างจริง: กด `V` แล้วกด `j` สองที -> เลือกได้ 3 บรรทัด จากนั้นกด `d` เพื่อลบทั้ง 3 บรรทัดรวด หรือกด `y` เพื่อคัดลอก ใน config นี้ยังผูกเพิ่มให้ใน Visual mode กด `J`/`K` เพื่อ **ย้ายบรรทัดที่เลือกขึ้น-ลง** ได้ด้วย (สะดวกมากเวลาจัดเรียงโค้ด)

**Command mode** — กด `:` เพื่อพิมพ์คำสั่งที่แถบล่างสุดของจอ เช่น `:w` (save), `:q` (quit), `:wq` (save แล้ว quit) พิมพ์เสร็จกด `<CR>` (Enter) เพื่อรัน ถ้าเปลี่ยนใจกด `<Esc>` ยกเลิกได้ คำสั่งที่ขึ้นต้นด้วยตัวพิมพ์ใหญ่ก็เรียกจากที่นี่ เช่น `:Mason`, `:LazyGit`, `:VimBeGood`

**Terminal mode** — config นี้มีเทอร์มินัลฝังในตัว (บน macOS/Linux คือ `$SHELL` ของคุณ ปกติคือ zsh ส่วนบน Windows เป็น PowerShell) เปิดด้วย `<A-w>` (แบบแบ่งครึ่งจอ) หรือ `<A-q>` (แบบลอย) ข้อควรระวัง: พอเข้าไปในเทอร์มินัลแล้ว การพิมพ์จะถูกส่งเข้าเทอร์มินัลหมด ถ้าอยากออกมาคุม Neovim ตามปกติให้กด `<C-\><C-n>` (Ctrl+Backslash แล้วตามด้วย Ctrl+n) — จำชุดนี้ไว้ ไม่งั้นจะรู้สึกเหมือนติดอยู่ในเทอร์มินัล

### 1.4 which-key: ครูสอนที่ดีที่สุดของคุณ

ถ้าจะให้จำได้อย่างเดียวจากหมวดนี้ ขอให้จำเรื่องนี้ครับ

config มีปลั๊กอินชื่อ **which-key.nvim** ที่จะคอย "ใบ้คำ" ให้คุณ วิธีใช้ง่ายมาก:

```
แตะ <Space> (กดครั้งเดียว) แล้วรอประมาณ 1 วินาที
```

จะมีเมนู pop-up โผล่ขึ้นมาที่ด้านล่างจอ แสดง **รายการ leader keymap ทั้งหมด** ว่ากด Space แล้วตามด้วยปุ่มไหน จะทำอะไรได้บ้าง เช่นคุณจะเห็น `e -> file tree`, `f -> find files`, `gg -> LazyGit` ฯลฯ

นี่คือเหตุผลว่าทำไมคุณ **ไม่จำเป็นต้องท่องจำ keymap ทั้งหมด** ตั้งแต่วันแรก แค่จำว่า "ทุกอย่างเริ่มที่ Space" แล้วปล่อยให้ which-key เตือนความจำ ยิ่งใช้บ่อยนิ้วก็จะจำเอง จนวันนึงคุณจะกดโดยไม่ต้องรอเมนูขึ้นเลยด้วยซ้ำ

ตัวอย่างจริงลองเล่นตอนนี้เลย: แตะ `<Space>` (กดครั้งเดียว) แล้วรอประมาณ 1 วินาที -> เห็นเมนู -> กด `e` -> file tree เปิดขึ้นมาทางซ้าย เห็นไหมครับว่าไม่ต้องจำอะไรเลย

### 1.5 วิธีอ่านคู่มือนี้

- **อ่านไปพร้อมลงมือทำ** อย่าอ่านเฉยๆ ให้เปิด Neovim ไว้ข้างๆ แล้วลองกดตามทุกตัวอย่าง ทักษะ Neovim สร้างจากนิ้ว ไม่ใช่จากตา
- **ตารางคือ keymap อ้างอิง** ส่วน code block (` ``` `) คือลำดับปุ่มหรือคำสั่งที่ให้กดจริง
- **หลงเมื่อไหร่กด `<Esc>`** เพื่อกลับ Normal mode (จาก terminal ใช้ `<C-\><C-n>`) แล้วเมื่ออยู่ใน Normal mode `<Esc>` ยังล้างไฮไลต์ผลการค้นหา + ไฮไลต์ `f`/`t` ของ flash ให้ด้วย — ถ้าเพิ่งออกจาก Insert/Visual อาจต้องกด `<Esc>` อีกครั้งหนึ่ง แล้วถ้าลืม leader keymap ก็แตะ `<Space>` (กดครั้งเดียว) แล้วรอประมาณ 1 วินาที เรียก which-key
- **ไม่ต้องรีบ** โหมดต่างๆ จะเริ่มเป็นธรรมชาติเองภายในไม่กี่วัน ตอนแรกอาจเผลอพิมพ์ตัวอักษรใน Normal mode แล้วงงว่าทำไมโค้ดเพี้ยน — นั่นเป็นเรื่องปกติของทุกคน กด `u` (undo) แล้วลองใหม่ได้เสมอ

พร้อมแล้วไปหมวดถัดไปกันเลยครับ เราจะเริ่มจากการเคลื่อนที่และแก้ไขข้อความใน Normal mode ซึ่งเป็นหัวใจที่ทำให้ Neovim เร็วอย่างที่ใครๆ เล่าลือกัน

---

## 2. พื้นฐาน Vim ที่ต้องแม่น (รากฐานสู่ expert)

ก่อนจะไปสนุกกับ plugin เท่ๆ อย่าง Telescope, LSP, หรือ Grapple เราต้องปูรากฐานให้แน่นก่อนนะครับ เพราะ "พลังที่แท้จริง" ของ Neovim ไม่ได้อยู่ที่ปุ่มลัดของ plugin แต่อยู่ที่ **ภาษาในการแก้ไขข้อความ (editing language)** ที่ Vim สร้างไว้

หัวใจของมันมีแค่ประโยคเดียว จำให้ขึ้นใจ:

```
[count][operator][motion หรือ text object]
```

พอเข้าใจไวยากรณ์นี้ คุณจะไม่ต้อง "ท่องจำ" ปุ่มลัดเป็นร้อยๆ อีกต่อไป เพราะทุกอย่าง **ประกอบกันได้ (composable)** เหมือนต่อเลโก้ ถ้ารู้ว่า `d` = ลบ และ `w` = ไปคำถัดไป คุณก็เดาได้เองว่า `dw` = ลบทั้งคำ ค่อยๆ อ่านหมวดนี้แล้วลองมือทีละอย่างนะครับ อย่ารีบ

> เกร็ดสำคัญ: ในคอนฟิกนี้ตั้ง `relativenumber` ไว้ (เห็นเลขบรรทัดนับจากตำแหน่งเคอร์เซอร์) เพราะมันช่วยเรื่อง count มาก เช่นเห็นเลข `5` อยู่ข้างบน ก็กด `5k` กระโดดขึ้นไปได้ทันที

---

### 2.1 Motions -- การเคลื่อนที่ (นี่คือ "คำนาม" ของประโยค)

Motion คือการบอกว่า "เคอร์เซอร์จะไปไหน" ใช้เดี่ยวๆ เพื่อเลื่อนก็ได้ หรือเอาไปต่อท้าย operator เพื่อบอกขอบเขตก็ได้ ลองฝึกใน normal mode

**เคลื่อนที่ระดับตัวอักษร / คำ**

| ปุ่ม | ความหมาย |
|------|-----------|
| `h` `j` `k` `l` | ซ้าย / ลง / ขึ้น / ขวา (มือขวาวางบน home row พอดี) |
| `w` | ไปต้น "คำ" ถัดไป (word) |
| `e` | ไป "ท้ายคำ" ถัดไป (end) |
| `b` | ถอยไปต้นคำก่อนหน้า (back) |
| `W` `E` `B` | เหมือน `w`/`e`/`b` แต่นับ "คำใหญ่" (WORD) คั่นด้วยช่องว่างเท่านั้น |

ความต่างของ `w` (ตัวเล็ก) กับ `W` (ตัวใหญ่) สำคัญมาก ลองดูบรรทัดนี้:

```
foo-bar.baz
```

- วางเคอร์เซอร์ที่ `f` แล้วกด `w` -> เด้งไปที่ `-` (เพราะ `-` และ `.` ถือเป็นตัวคั่นคำ)
- แต่กด `W` -> ข้ามทั้งก้อนไปเลย เพราะ WORD มองว่า `foo-bar.baz` เป็นคำเดียว (ไม่มีช่องว่างคั่น)

**เคลื่อนที่ในบรรทัด**

| ปุ่ม | ความหมาย |
|------|-----------|
| `0` | ไปต้นบรรทัด (คอลัมน์แรกสุด แม้เป็นช่องว่าง) |
| `^` | ไปตัวอักษรแรกที่ไม่ใช่ช่องว่าง (มักใช้บ่อยกว่า `0`) |
| `$` | ไปท้ายบรรทัด |

**กระโดดหาตัวอักษรในบรรทัด -- `f/F/t/T`** (พวกนี้ทรงพลังมาก ฝึกให้คล่อง)

| ปุ่ม | ความหมาย |
|------|-----------|
| `f{char}` | กระโดด "ไปทับ" ตัวอักษรถัดไปที่ตรงกัน (find) |
| `F{char}` | เหมือน `f` แต่หา "ย้อนหลัง" |
| `t{char}` | กระโดดไป "ก่อนถึง" ตัวอักษรนั้น 1 ช่อง (till) |
| `T{char}` | เหมือน `t` แต่ย้อนหลัง |
| `;` | ทำซ้ำ f/F/t/T ครั้งล่าสุด "ทิศเดิม" |
| `,` | ทำซ้ำ f/F/t/T ครั้งล่าสุด "ทิศตรงข้าม" |

ตัวอย่าง สมมติบรรทัด `let name = getUserName(id)` เคอร์เซอร์อยู่ต้นบรรทัด:

```
กด f(   -> เคอร์เซอร์เด้งไปทับวงเล็บเปิด (
กด ;    -> ไม่มีตัวถัดไป ก็อยู่ที่เดิม
กด F=   -> ย้อนกลับไปทับเครื่องหมาย =
```

พลังที่แท้จริงมาตอนจับคู่กับ operator เช่น `dt)` = ลบไปจนถึงก่อนวงเล็บปิด, `ci(` (จะพูดถึง text object ในหมวดถัดไป) ส่วน `;` กับ `,` ทำให้กระโดดหลายจุดโดยไม่ต้องพิมพ์ `f{char}` ใหม่

**เคลื่อนที่ระดับไฟล์ / หน้าจอ**

| ปุ่ม | ความหมาย |
|------|-----------|
| `gg` | ไปบรรทัดแรกสุดของไฟล์ |
| `G` | ไปบรรทัดสุดท้ายของไฟล์ |
| `{n}G` | กระโดดไปบรรทัดที่ n เช่น `42G` ไปบรรทัด 42 |
| `<C-d>` | เลื่อนลงครึ่งหน้าจอ (down) |
| `<C-u>` | เลื่อนขึ้นครึ่งหน้าจอ (up) |
| `H` | ไปบรรทัดบนสุดของ "หน้าจอ" (High) |
| `M` | ไปบรรทัดกลางหน้าจอ (Middle) |
| `L` | ไปบรรทัดล่างสุดของหน้าจอ (Low) |
| `%` | กระโดดไปวงเล็บ/ปีกกา/bracket ที่คู่กัน |

`%` มีประโยชน์มากเวลาอ่านโค้ด วางเคอร์เซอร์ที่ `{` แล้วกด `%` มันจะพาไป `}` ที่ปิดคู่กันทันที ช่วยตรวจว่า block ไหนจบตรงไหน

> เคล็ดลับ: คอนฟิกนี้ตั้ง `scrolloff = 8` ไว้ แปลว่า Neovim จะพยายามเว้นบริบทด้านบน/ล่างประมาณ 8 บรรทัดเมื่อทำได้ (ยกเว้นบริเวณต้นหรือท้ายไฟล์) คอยเลื่อนจอให้เห็นบริบทรอบตัวเสมอ ทำให้อ่านโค้ดสบายตาขึ้นเยอะ

---

### 2.2 Operators -- การกระทำ (นี่คือ "คำกริยา" ของประโยค)

Operator คือ "จะทำอะไร" มันจะรอ motion หรือ text object มาบอก "ทำกับตรงไหน" ต่อท้าย

| Operator | ความหมาย |
|----------|-----------|
| `d` | delete (ลบ แล้วเก็บลง register) |
| `c` | change (ลบ + เข้า insert mode ทันที) |
| `y` | yank (คัดลอก ไม่ลบ) |
| `>` | เพิ่ม indent (เยื้องเข้า) |
| `<` | ลด indent (เยื้องออก) |
| `=` | auto-indent (จัดย่อหน้าให้อัตโนมัติ) |
| `gu` | แปลงเป็นตัวพิมพ์เล็ก (lowercase) |
| `gU` | แปลงเป็นตัวพิมพ์ใหญ่ (uppercase) |

จุดที่สวยงามคือ operator ทุกตัวใช้ไวยากรณ์เดียวกัน พอรู้ว่า `d` ต่อกับ motion ได้ ก็แปลว่า `c`, `y`, `>`, `=`, `gU` ก็ต่อได้เหมือนกันหมด

ตัวอย่างจริง:

```
dw    -> ลบตั้งแต่เคอร์เซอร์ไปจนต้นคำถัดไป
cw    -> เปลี่ยนจากเคอร์เซอร์ถึงท้ายคำ; ถ้าต้องการทั้งคำไม่ว่าเคอร์เซอร์อยู่ตรงไหน ใช้ ciw
y$    -> คัดลอกจากเคอร์เซอร์ไปจนท้ายบรรทัด
d$    -> ลบจากเคอร์เซอร์ไปจนท้ายบรรทัด
gU$   -> เปลี่ยนจากเคอร์เซอร์ถึงท้ายบรรทัดเป็นตัวใหญ่ทั้งหมด
```

**ทริกกดซ้ำ operator = ทำทั้งบรรทัด** ถ้ากด operator สองครั้ง มันหมายถึง "ทั้งบรรทัดนี้":

| ปุ่ม | ผล |
|------|-----|
| `dd` | ลบทั้งบรรทัด |
| `cc` | ลบเนื้อในบรรทัด (คง indent) แล้วเข้า insert |
| `yy` | คัดลอกทั้งบรรทัด |
| `>>` | เยื้องบรรทัดเข้า 1 ระดับ |
| `==` | auto-indent บรรทัดปัจจุบัน |

> หมายเหตุเรื่อง indent: คอนฟิกนี้ตั้ง `shiftwidth = 2`, `expandtab = true` ดังนั้น `>>` จะเยื้องเข้า 2 ช่อง (เป็น space ไม่ใช่ tab) และเปิด `smartindent` ไว้ ทำให้ Vim เดา indent ตอนขึ้นบรรทัดใหม่ให้ได้พอสมควร

---

### 2.3 ไวยากรณ์เต็ม: [count][operator][motion] -- หัวใจที่ทำให้คุณเป็น expert

ทีนี้มารวมร่างกัน สูตรคือ **จำนวนครั้ง + จะทำอะไร + ทำกับตรงไหน** อ่านออกเสียงเป็นประโยคภาษาอังกฤษได้เลย

```
d2w   -> "delete 2 words"           ลบ 2 คำถัดไป
c3j   -> "change 3 lines down"      ลบบรรทัดนี้ + 3 บรรทัดล่าง แล้วเข้า insert
>ip   -> "indent inner paragraph"   เยื้องทั้งย่อหน้าที่เคอร์เซอร์อยู่เข้าไป
y$    -> "yank to end of line"      คัดลอกถึงท้ายบรรทัด
2dd   -> "delete 2 lines"           ลบ 2 บรรทัด
d5G   -> "delete to line 5"         ลบจากบรรทัดนี้ไปถึงบรรทัด 5
=ip   -> auto-indent ทั้งย่อหน้า
```

count จะวางหน้า operator (`2dw`) หรือหน้า motion (`d2w`) ก็ให้ผลเหมือนกัน เลือกที่ถนัด

ลองคิดเป็นภาษา แล้วมือจะพิมพ์ออกมาเองโดยไม่ต้องท่อง เช่นอยากลบ 3 คำ -> "delete 3 words" -> `d3w` เห็นไหมครับว่ามันเป็นธรรมชาติมาก นี่คือเหตุผลที่คนใช้ Vim คล่องแล้วไม่อยากกลับไปใช้ editor อื่น

> ในหมวดถัดไปเราจะแทน motion ด้วยสิ่งที่เรียกว่า **text object** (เช่น `iw`, `i(`, `ip`) ซึ่งทรงพลังกว่า motion ธรรมดามาก แต่ตอนนี้ขอให้แม่นไวยากรณ์กับ built-in text object พื้นฐานก่อน (Vim มี text object ในตัวอยู่แล้ว ส่วน "function/class text object" อย่าง `vaf`/`vif` config นี้เพิ่ม `nvim-treesitter-textobjects` ให้ใช้ได้แล้ว — ดู §3)

---

### 2.4 Search -- ค้นหาในไฟล์

การค้นหาก็เป็น motion อย่างหนึ่ง เอาไปต่อกับ operator ได้ด้วย

| ปุ่ม | ความหมาย |
|------|-----------|
| `/foo` | ค้นหา `foo` เดินหน้า (แล้วกด `<CR>`) |
| `?foo` | ค้นหา `foo` ย้อนหลัง |
| `n` | ไปผลลัพธ์ถัดไป (ทิศเดิม) |
| `N` | ไปผลลัพธ์ก่อนหน้า (ทิศตรงข้าม) |
| `*` | ค้นหา "คำใต้เคอร์เซอร์" เดินหน้า (ไม่ต้องพิมพ์เอง) |
| `#` | ค้นหาคำใต้เคอร์เซอร์ย้อนหลัง |

ตัวอย่าง: วางเคอร์เซอร์บนตัวแปร `count` แล้วกด `*` -> Vim จะไฮไลต์และกระโดดไปที่ `count` ตัวถัดไปทันที กด `n` ไล่ดูทุกจุดที่ใช้ตัวแปรนี้ได้เลย เหมาะมากเวลาไล่อ่านโค้ด

คอนฟิกนี้เปิด `incsearch` (เห็นผลลัพธ์ทันทีระหว่างพิมพ์) และ `hlsearch` (ไฮไลต์ทุกจุดที่เจอ) ไว้ พอค้นเสร็จ ไฮไลต์จะค้างอยู่ ให้ **กด `<Esc>` เพื่อล้างไฮไลต์** (คอนฟิกนี้ผูก `<Esc>` ใน normal mode ให้สั่ง `:nohlsearch` ให้แล้ว สะดวกมาก)

เท่ๆ อีกอย่าง: `/` เป็น motion ต่อกับ operator ได้ เช่น `d/foo<CR>` = ลบจากเคอร์เซอร์ไปจนถึงตำแหน่งที่เจอ `foo` ครั้งแรก

---

### 2.5 Undo / Redo และ "จุด" -- พลังพิเศษที่มืออาชีพขาดไม่ได้

| ปุ่ม | ความหมาย |
|------|-----------|
| `u` | undo (ย้อนกลับ) |
| `<C-r>` | redo (ทำซ้ำสิ่งที่เพิ่ง undo) |
| `.` | **dot command -- ทำซ้ำคำสั่งแก้ไขล่าสุด** |

> โบนัส: คอนฟิกนี้เปิด `undofile = true` และตั้ง `undodir` ไว้ แปลว่าประวัติ undo จะถูกบันทึกลงดิสก์ **ปิดไฟล์แล้วเปิดใหม่ก็ยัง `u` ย้อนได้อยู่** ไม่หายไปไหน อุ่นใจมาก

**dot command (`.`) คือหนึ่งในของขวัญที่ดีที่สุดของ Vim** จำง่ายๆ ว่ามัน "เล่นซ้ำคำสั่งแก้ไขล่าสุดที่คุณเพิ่งทำ" ลองดูสถานการณ์จริง สมมติต้องเปลี่ยนคำว่า `oldName` เป็น `newName` หลายที่ในไฟล์:

```
1. วางเคอร์เซอร์บน oldName ตัวแรก
2. กด cw แล้วพิมพ์ newName แล้วกด <Esc>   (นี่คือ "คำสั่งแก้ไขล่าสุด")
3. กด n เพื่อกระโดดไป oldName ตัวถัดไป (ถ้าค้นด้วย * ไว้ก่อน)
4. กด .  -> Vim ทำซ้ำ cw+newName ให้อัตโนมัติ!
5. กด n แล้ว . สลับกันไปเรื่อยๆ
```

เห็นไหมครับว่าแทบไม่ต้องพิมพ์อะไรซ้ำเลย นี่คือรูปแบบ `n` แล้ว `.` ที่คน Vim ใช้กันจนติดมือ พอคุณเริ่มคิดแบบ "ทำครั้งแรกให้ดี แล้วปล่อยให้ `.` ทำงานที่เหลือ" คุณจะแก้โค้ดได้เร็วขึ้นเป็นเท่าตัว

> เคล็ดลับ expert: พยายามทำให้การแก้ไขแต่ละครั้งเป็น "หน่วยเดียวที่ทำซ้ำได้" เช่นใช้ `cw`, `ciw`, `A;<Esc>` (เติม ; ท้ายบรรทัด) แทนที่จะกดลูกศรจิ้มไปมา เพราะยิ่งคำสั่งกระชับ `.` ก็ยิ่งทรงพลัง

---

### 2.6 การเข้า Insert mode -- มีตั้งหลายวิธี เลือกให้เหมาะ

หลายคนใช้แค่ `i` แต่จริงๆ Vim ให้เราเข้า insert mode ได้หลายทางเพื่อประหยัดการกดปุ่มเลื่อนเคอร์เซอร์

| ปุ่ม | ความหมาย |
|------|-----------|
| `i` | insert ก่อนเคอร์เซอร์ |
| `I` | insert ที่ต้นบรรทัด (ตัวอักษรแรกที่ไม่ใช่ช่องว่าง) |
| `a` | insert หลังเคอร์เซอร์ (append) |
| `A` | insert ที่ท้ายบรรทัด |
| `o` | เปิดบรรทัดใหม่ "ข้างล่าง" แล้วเข้า insert |
| `O` | เปิดบรรทัดใหม่ "ข้างบน" แล้วเข้า insert |
| `s` | ลบตัวอักษรใต้เคอร์เซอร์ 1 ตัว แล้วเข้า insert (substitute) |
| `S` | ลบทั้งบรรทัด (คง indent) แล้วเข้า insert (เหมือน `cc`) |

> ⚠️ **`s`/`S` ถูก flash จองไปทำ jump แล้ว** (ดู §7.8) — ถ้าอยากได้ substitute เดิม ใช้ `cl` (แทน `s`) และ `cc` (แทน `S`) ได้ผลเหมือนกัน

ตัวอย่างที่เจอบ่อยในชีวิตจริง:

```
A;<Esc>   -> กระโดดไปท้ายบรรทัดแล้วเติม ; ปิดท้าย
o         -> อยากเขียนโค้ดบรรทัดใหม่ต่อจากบรรทัดนี้ ไม่ต้องกด End+Enter
I// <Esc> -> ไปต้นบรรทัดแล้วใส่ // นำหน้า (แม้จริงๆ comment ใช้ gcc สะดวกกว่า)
```

จำง่ายๆ: ตัวเล็ก (`i a o s`) ทำงานแบบ "ตรงจุด" ส่วนตัวใหญ่ (`I A O S`) ทำงานแบบ "ระดับบรรทัด/สุดขอบ"

> ข้อควรรู้ในคอนฟิกนี้: มี plugin **mini.pairs** ช่วยปิดวงเล็บ/quote ให้อัตโนมัติ เช่นพิมพ์ `(` แล้วมันเติม `)` ให้เอง และมี **blink.cmp** ที่ป๊อปเมนู completion ขึ้นมาเองระหว่างพิมพ์ (กด `<C-n>`/`<C-p>` เลือก, `<CR>` ยืนยัน) เดี๋ยวเราจะลงรายละเอียดพวกนี้ในหมวดถัดๆ ไป ตอนนี้แค่รู้ว่าตอนอยู่ใน insert mode จะมีเพื่อนช่วยพิมพ์อยู่

---

### 2.7 สรุป: ทำไมทุกอย่าง "ประกอบกันได้"

ขอย้ำแนวคิดหลักอีกครั้ง เพราะนี่คือกุญแจสู่ความเป็น expert จริงๆ ครับ: คุณไม่ได้ท่องจำ "คำสั่ง" เป็นพันๆ อัน แต่คุณเรียน **คำศัพท์เล็กๆ ไม่กี่คำ** แล้วเอามันมาประกอบเป็นประโยคได้ไม่รู้จบ

```
operator (d/c/y/>/=/gU) 
   +
motion (w/e/b/$/f/G/%) หรือ text object (iw/i(/ip)
   +
count (ตัวเลขนำหน้า)
   =
ความสามารถนับไม่ถ้วน
```

- รู้ `d` + รู้ `w`  -> ได้ `dw`
- รู้ `d` + รู้ `t)` -> ได้ `dt)` (ลบถึงก่อนวงเล็บปิด)
- รู้ `d` + รู้ `G`  -> ได้ `dG` (ลบถึงท้ายไฟล์)
- รู้ `y` + รู้ `%`  -> ได้ `y%` (คัดลอกทั้ง block ในวงเล็บคู่)
- เพิ่ม count -> `d2w`, `y3j`, `>4>`... ได้หมด
- ทำครั้งแรกให้สวย แล้วปล่อย `.` ทำซ้ำ

**การบ้านเล็กๆ:** เปิดไฟล์โค้ดขึ้นมาสักไฟล์ แล้วลองฝึกวันละนิด เช่นวันนี้เอาแค่ `f`/`t` + `;`/`,` ให้คล่อง พรุ่งนี้ค่อยเพิ่ม `ciw` กับ `.` เข้าไป ไม่ต้องรีบเก่งในวันเดียว รากฐานพวกนี้แหละที่จะทำให้ทุกหมวดถัดไป (Telescope, LSP, Git) ใช้ได้ลื่นขึ้นหลายเท่า และถ้าอยากฝึกแบบเกม คอนฟิกนี้มี **vim-be-good** ให้เล่นด้วย (สั่ง `:VimBeGood`) สนุกและได้ฝึก motion ไปในตัวครับ

---

## 3. Text Objects — หัวใจของการแก้โค้ดเร็ว

ถ้าจะมีบทเดียวที่เปลี่ยนคุณจาก "คนพิมพ์ในโปรแกรม vi" ไปเป็น "คนใช้ Vim เป็น" บทนี้คือบทนั้นครับ Text object คือสิ่งที่ทำให้ Vim ต่างจาก editor อื่นทั้งหมด และพอเข้าใจแล้วคุณจะแก้โค้ดได้เร็วแบบที่ตัวเองก็ทึ่ง

### แนวคิด: ทำงานกับ "โครงสร้าง" ไม่ใช่ตัวอักษร

editor ทั่วไปคิดเป็น "ตัวอักษร" กับ "บรรทัด" อยากลบคำในวงเล็บก็ต้องเอาเมาส์ไปลากคลุม แล้วกด Delete ทีละที แต่ Vim คิดคนละแบบ — Vim มองโค้ดเป็น **โครงสร้างที่มีความหมาย** คำ (word), ข้อความในเครื่องหมายคำพูด (quote), สิ่งที่อยู่ในวงเล็บ (parentheses), บล็อกในปีกกา (braces), ย่อหน้า (paragraph), แท็ก HTML

Text object คือชื่อเรียกโครงสร้างเหล่านั้น และหัวใจของมันคือสูตรนี้

```
operator + text object
```

`operator` คือ "จะทำอะไร" (เช่น `d` = delete, `c` = change, `y` = yank, `v` = เลือกใน visual) ส่วน `text object` คือ "ทำกับอะไร" คุณเคยเรียน `d` + `motion` มาแล้ว (เช่น `dw` = ลบไปถึงคำถัดไป) — text object ก็คือ "เป้าหมาย" อีกประเภทที่เสียบเข้าไปแทน motion ได้เลย ความมหัศจรรย์คือ **คุณไม่ต้องวางเคอร์เซอร์ตรงจุดเริ่ม** ขอแค่เคอร์เซอร์อยู่ "ข้างใน" โครงสร้างนั้น Vim จะจัดการขอบเขตให้เอง

ตัวอย่างที่เห็นภาพชัดที่สุด สมมติมีบรรทัด

```python
name = "John"
```

วางเคอร์เซอร์ตรงไหนก็ได้ในคำว่า `John` (จะเป็น `J`, `o`, หรือ `n` ก็ได้) แล้วกด `ci"` — Vim ลบ `John` ทิ้ง แล้วพาคุณเข้า insert mode ตรงกลางเครื่องหมายคำพูดพอดี พร้อมพิมพ์ชื่อใหม่ทันที คุณไม่ต้องขยับไปหาขอบ ไม่ต้องนับตัวอักษร นี่แหละคือ "ทำงานกับโครงสร้าง"

### ความต่างระหว่าง `i` (inner) กับ `a` (around)

text object เกือบทุกตัวมาเป็นคู่ ขึ้นต้นด้วย `i` หรือ `a`

- **`i` = inner** เอาเฉพาะ **ข้างใน** ไม่รวมขอบเขต (ไม่เอาเครื่องหมายคำพูด ไม่เอาวงเล็บ ไม่เอาช่องว่างรอบข้าง)
- **`a` = around** เอา **รวมขอบเขต** ทั้งหมด (เอาเครื่องหมายคำพูด/วงเล็บ/ปีกกาติดมาด้วย และมักรวมช่องว่างที่ตามมา)

จำง่ายๆ ว่า `i` ย่อมาจาก **inner/inside** และ `a` ย่อมาจาก **a / around** เทียบให้เห็นชัดกับ

```python
greet = "hello"
```

วางเคอร์เซอร์บนคำว่า `hello`

| กด | ผลลัพธ์ที่เลือก/ลบ | เหลืออะไร |
|---|---|---|
| `di"` | `hello` (เฉพาะข้างใน) | `greet = ""` |
| `da"` | `"hello"` (รวมเครื่องหมายคำพูด) | `greet = ` |

เห็นความต่างไหมครับ — `di"` เก็บเครื่องหมายคำพูดไว้ให้ (เหมาะกับ "เปลี่ยนข้อความข้างใน") ส่วน `da"` กวาดทิ้งทั้งชุด (เหมาะกับ "ลบทั้งก้อนออกไปเลย") กฎเดียวกันนี้ใช้กับ text object ทุกตัวในตารางข้างล่าง

### ตารางรวม Text Objects ที่ใช้บ่อย

ทั้งหมดนี้เป็นของ **built-in ใน Vim/Neovim** ไม่ต้องลง plugin อะไรเพิ่ม มันอยู่กับคุณตั้งแต่ติดตั้งเสร็จ

| Text object | จับอะไร | ตัวอย่างจริง |
|---|---|---|
| `iw` / `aw` | word (`aw` รวมช่องว่างข้างๆ) | `ciw` = เปลี่ยนคำที่เคอร์เซอร์ทับอยู่ |
| `iW` / `aW` | WORD (นับจนเจอ whitespace เช่น `foo-bar.baz` ทั้งก้อน) | `diW` = ลบ token ยาวๆ ที่มีเครื่องหมายปน |
| `i"` / `a"` | ในเครื่องหมาย `"` | `ci"` = เปลี่ยน string ในอัญประกาศคู่ |
| `i'` / `a'` | ในเครื่องหมาย `'` | `di'` = ลบข้อความในอัญประกาศเดี่ยว |
| `` i` `` / `` a` `` | ใน backtick `` ` `` | `` yi` `` = คัดลอกข้อความใน backtick |
| `i(` `i)` `ib` / `a(` `a)` `ab` | ในวงเล็บ `( )` | `di(` = ลบทุกอย่างในวงเล็บ |
| `i{` `i}` `iB` / `a{` `a}` `aB` | ในปีกกา `{ }` | `ca{` = ลบทั้งบล็อกปีกกา (รวมปีกกา) แล้วเข้า insert |
| `i[` `i]` / `a[` `a]` | ในวงเล็บเหลี่ยม `[ ]` | `yi[` = คัดลอกสิ่งที่อยู่ใน list/index |
| `i<` `i>` / `a<` `a>` | ในวงเล็บมุม `< >` | `di<` = ลบเนื้อใน generic/tag angle |
| `it` / `at` | tag ของ HTML/XML (`it`=เนื้อใน, `at`=รวมแท็กเปิด-ปิด) | `cit` = เปลี่ยนข้อความในแท็ก |
| `ip` / `ap` | paragraph (ย่อหน้า คั่นด้วยบรรทัดว่าง) | `dap` = ลบทั้งย่อหน้า |
| `is` / `as` | sentence (ประโยค) | `das` = ลบทั้งประโยค |

หมายเหตุ 3 อย่างที่ควรรู้

1. **วงเล็บพิมพ์ตัวไหนก็ได้** `i(` กับ `i)` ให้ผลเหมือนกัน `i{` กับ `i}` ก็เหมือนกัน เลือกอันที่มือคุณสะดวก
2. **ตัวย่อ `b` และ `B`** `ib`/`ab` = เหมือน `i(`/`a(` (bracket วงเล็บกลม), `iB`/`aB` = เหมือน `i{`/`a{` (Big bracket ปีกกา) จำได้แล้วพิมพ์เร็วขึ้นมาก
3. **`it`/`at` เก่งเรื่องแท็ก** ใช้ได้ดีในไฟล์ HTML/XML/JSX เพราะ Vim เข้าใจโครงสร้างแท็กเปิด-ปิด

### ตัวอย่างใช้จริงแบบจับมือทำ

**เปลี่ยนคำ — `ciw`**
```python
def calculate_total(items):
```
วางเคอร์เซอร์ที่ไหนก็ได้บนคำว่า `total` แล้วกด `ciw` → `total` หายไป เคอร์เซอร์รออยู่ตรงนั้น พิมพ์ `sum` ได้เลย กลายเป็น `def calculate_sum(items):` นี่คือคอมโบที่คุณจะใช้บ่อยที่สุดในชีวิต Vim

**เปลี่ยน string — `ci"`**
```python
message = "old text"
```
เคอร์เซอร์บน `old text` (ตรงไหนก็ได้ในเครื่องหมายคำพูด) → `ci"` → เหลือ `message = ""` พร้อมเคอร์เซอร์อยู่กลางอัญประกาศ พิมพ์ข้อความใหม่ได้ทันที

**ลบ argument ในวงเล็บ — `di(`**
```python
print("debug", value, extra)
```
เคอร์เซอร์อยู่ที่ไหนก็ได้ในวงเล็บ → `di(` → เหลือ `print()` เนื้อในหายเกลี้ยง แต่วงเล็บยังอยู่ให้พิมพ์ต่อ

**ลบทั้งบล็อกปีกกา (รวมปีกกา) — `ca{`**
```go
if err != nil {
    log.Fatal(err)
}
```
วางเคอร์เซอร์ตรงไหนก็ได้ในบล็อก → `ca{` → ทั้ง `{ ... }` หายไปหมด แล้วเข้า insert mode พร้อมเขียนบล็อกใหม่ (ถ้าใช้ `ci{` แทน จะเก็บปีกกาไว้แล้วล้างแค่ข้างใน)

**คัดลอกเนื้อในแท็ก — `yit`**
```html
<h1>Welcome home</h1>
```
เคอร์เซอร์อยู่บน `Welcome home` → `yit` → คัดลอก `Welcome home` (ไม่เอาแท็ก) เก็บไว้ใน register พร้อมเอาไปวางที่อื่น

**เปลี่ยนเนื้อในแท็ก — `cit`**
```html
<button>Click</button>
```
เคอร์เซอร์บน `Click` → `cit` → ลบ `Click` เข้า insert mode พิมพ์ `Submit` → กลายเป็น `<button>Submit</button>` โดยแท็กไม่ถูกแตะเลย

**ลบทั้งย่อหน้า — `dap`**
```
บรรทัดแรกของย่อหน้า
บรรทัดสองของย่อหน้า

ย่อหน้าถัดไป
```
เคอร์เซอร์อยู่ในย่อหน้าบน → `dap` → ทั้งย่อหน้า (รวมบรรทัดว่างที่ตามมา) หายไป เหลือแค่ย่อหน้าถัดไป เหมาะมากเวลาลบฟังก์ชันสั้นๆ หรือบล็อก comment ทั้งก้อน

**เลือกดูก่อน (ปลอดภัยกว่า) — `vi(`**
ถ้ายังไม่มั่นใจว่า text object จะคลุมถึงไหน ให้ใช้ `v` นำหน้าเพื่อ "เลือกให้เห็นก่อน" เช่น `vi(` จะ highlight ทุกอย่างในวงเล็บให้คุณเห็นด้วยตา ถ้าถูกใจค่อยกด `d` ลบ หรือ `y` คัดลอกต่อ เป็นวิธีฝึกที่ดีมากตอนเริ่มต้น เพราะเห็นขอบเขตชัดก่อนลงมือ

### ผสมกับ count (ทำงานทีละหลายชั้น)

text object ผสมกับตัวเลข count ได้ เพื่อขยายขอบเขตออกไปหลายชั้น เช่นโค้ดวงเล็บซ้อนวงเล็บ

```python
result = foo(bar(baz, 1), 2)
```

- เคอร์เซอร์อยู่บน `baz` แล้วกด `di(` → ลบแค่ในวงเล็บชั้นในสุด เหลือ `foo(bar(), 2)`
- แต่ถ้ากด `d2i(` (count = 2) → ทะลุออกไปวงเล็บชั้นนอกอีกชั้น เหลือ `foo()`

กับย่อหน้าก็ใช้ได้ `d2ap` = ลบสองย่อหน้ารวด `y3ip` = คัดลอกสามย่อหน้า count ช่วยให้คุณ "ซูมออก" ทีละระดับได้อย่างแม่นยำ

### ⭐ function/class text object — เปิดใช้ได้แล้ว (nvim-treesitter-textobjects)

นอกจาก text object พื้นฐานข้างบน config นี้ติดตั้ง **nvim-treesitter-textobjects** เพิ่ม ทำให้เลือก/แก้ตาม "โครงสร้างโค้ด" ได้ — สิ่งที่ Vim ตัวเปล่าทำไม่ได้ (`f` = function, `c` = class):

| ปุ่ม | ทำอะไร |
|------|--------|
| `vif` / `vaf` | เลือก **ในฟังก์ชัน** / **ทั้งฟังก์ชัน** |
| `vic` / `vac` | เลือก **ในคลาส** / **ทั้งคลาส** |
| `dif` / `daf` / `cif` | ลบไส้ / ลบทั้งฟังก์ชัน / เปลี่ยนไส้ (ผสม operator + count ได้ เช่น `d2af`) |
| `]f` / `[f` | กระโดดฟังก์ชัน **ถัดไป / ก่อนหน้า** |

ใช้ได้เฉพาะภาษาที่มี treesitter parser + textobjects query (lua/python/go/c/cpp/rust/bash/... ที่ config ลงไว้) และต้องเปิด **ไฟล์โค้ดจริง** ถึงจะเห็นผล (ไฟล์ `.md`/plain text ไม่มีฟังก์ชันให้จับ)

> **หมายเหตุ:** `nvim-treesitter-textobjects` เพิ่งเพิ่มเข้ามา (commit `54850b9`) — ก่อนหน้านี้ config ไม่มี function/class text object

**ข่าวดี:** text object built-in ในตารางก่อนหน้า (`iw`, `i"`, `i(`, `i{`, `it`, `ip` ฯลฯ) ยังเป็นพระเอกงานประจำวัน 90%+ — function/class object แค่เสริมตอนต้องจัดการทั้งฟังก์ชัน/คลาสรวดเดียว

### surround — เพิ่ม/เปลี่ยน/ลบ "ตัวคร่อม" (mini.surround)

อีกตัวที่เพิ่งเพิ่มคือ **mini.surround** — จัดการเครื่องหมายที่ห่อข้อความ (quote / วงเล็บ / HTML tag) โดยไม่ต้องขยับเคอร์เซอร์ไปที่ขอบ ใช้ prefix **`gs`** (เพราะ `s` ถูก flash จองไปทำ jump):

| ปุ่ม | ทำอะไร |
|------|--------|
| `gsa{motion}{ตัวคร่อม}` | **เพิ่ม** (ต้องใส่ motion) เช่น `gsaiw"` ห่อคำด้วย `"` · `gsaiw)` (ตัวปิด) = ไม่เว้นวรรค · `gsaiwt` แล้วพิมพ์ชื่อ tag = ห่อ HTML · (หรือเลือก visual ก่อนแล้ว `gsa"`) |
| `gsd{ตัวคร่อม}` | **ลบ** เช่น `gsd"` = เอา `"` ที่ห่ออยู่ออก |
| `gsr{เดิม}{ใหม่}` | **เปลี่ยน** เช่น `gsr"'` = เปลี่ยน `"` เป็น `'` |
| `gsf` / `gsF` | ไปที่ขอบ ขวา/ซ้าย ของตัวคร่อมที่ล้อมอยู่ · `gsh` = ไฮไลต์ตัวคร่อม |

> เคล็ด: ใช้ **ตัวปิด** (`)`/`]`/`}`) จะไม่เว้นวรรคด้านใน — `gsaiw)` ได้ `(word)` ส่วน `gsaiw(` ได้ `( word )`

---

## 4. Plugins: ค้นหาและนำทางไฟล์

พอโปรเจกต์เริ่มมีไฟล์เยอะขึ้น การเปิดไฟล์ด้วยการพิมพ์ path เต็มๆ มันช้าและน่าเบื่อ หมวดนี้คือหัวใจของการทำงานเร็วใน Neovim เลยครับ เราจะมีเครื่องมือ 4 ตัวที่ทำงานร่วมกันเป็นทีม: **nvim-tree** (ดูโครงสร้างไฟล์เหมือน sidebar), **telescope** (ค้นหาไฟล์/ข้อความแบบ fuzzy), **grapple** (คั่นหน้าไฟล์สำคัญไว้กระโดดไปมา) และ **project.nvim** (สลับโปรเจกต์). ค่อยๆ ทำความรู้จักทีละตัว แล้วตอนท้ายเราจะเอามาร้อยเป็น workflow จริงกัน

---

### 4.1 nvim-tree — ต้นไม้ไฟล์ (file explorer)

nvim-tree คือแผงด้านซ้ายที่โชว์โครงสร้างโฟลเดอร์/ไฟล์ทั้งหมด เหมาะเวลาที่เรายัง "ไม่รู้ว่าไฟล์อยู่ตรงไหน" หรืออยากเห็นภาพรวมของโปรเจกต์ หรืออยากสร้าง/ลบ/เปลี่ยนชื่อไฟล์แบบเห็นๆ

เปิด/ปิดด้วย:

| Keymap | ทำอะไร |
|--------|--------|
| `<Space>e` | เปิด/ปิดต้นไม้ไฟล์ (toggle) และเด้งเคอร์เซอร์ไปที่ไฟล์ปัจจุบันให้ด้วย |

> เกร็ด: `<Space>e` ในคอนฟิกนี้ใช้คำสั่ง `NvimTreeFindFileToggle` แปลว่าเวลาเปิด มันจะ **highlight ไฟล์ที่เรากำลังแก้อยู่** ในต้นไม้ให้เลย ทำให้รู้ทันทีว่าไฟล์นี้อยู่โฟลเดอร์ไหน สะดวกมากตอนหลงทาง

**ปุ่มข้างในต้นไม้ (default keys ของ nvim-tree)** — พวกนี้ใช้ได้เฉพาะตอนเคอร์เซอร์อยู่ในแผงต้นไม้:

| ปุ่ม | ทำอะไร |
|------|--------|
| `j` `k` | เลื่อนขึ้น/ลงในทรี |
| `<CR>` / `o` | เปิดไฟล์ที่เลือก (หรือกาง/ยุบโฟลเดอร์) |
| `<BS>` (Backspace) | ยุบโฟลเดอร์ที่กางอยู่ |
| `a` | สร้างไฟล์หรือโฟลเดอร์ใหม่ |
| `d` | ลบไฟล์/โฟลเดอร์ที่เลือก (มีถามยืนยัน) |
| `r` | เปลี่ยนชื่อ (rename) |
| `x` | ตัด (cut) — เตรียมย้าย |
| `c` | คัดลอก (copy) |
| `p` | วาง (paste) สิ่งที่ cut/copy ไว้ |
| `y` | คัดลอก **ชื่อไฟล์** (filename) เข้า system clipboard |
| `Y` | คัดลอก **relative path** เข้า system clipboard |
| `gy` | คัดลอก **absolute path** เข้า system clipboard |
| `<C-v>` | เปิดไฟล์ในหน้าต่างแยกแนวตั้ง (vertical split) |
| `<C-x>` | เปิดไฟล์ในหน้าต่างแยกแนวนอน (horizontal split) |
| `R` | รีเฟรชต้นไม้ (refresh) |
| `H` | เปิด/ปิดการซ่อนไฟล์ dotfiles (ไฟล์ที่ขึ้นต้นด้วย `.` เช่น `.git`, `.env`) |
| `s` | เปิดไฟล์/โฟลเดอร์ด้วยโปรแกรมของระบบ (system-open) |
| `g?` | โชว์ help — รายการปุ่มทั้งหมด (ลืมปุ่มไหนกดอันนี้ได้เสมอ) |

> เกร็ด: อย่าสับสน — `c`/`x`/`p` คือ **จัดการตัวไฟล์** (คัดลอก/ตัด/วางไฟล์จริงในระบบไฟล์) ส่วน `y`/`Y`/`gy` คือ **คัดลอก "ข้อความ path"** เข้า clipboard (เอาไปวางที่อื่นได้). ปุ่มชุด `y`/`Y`/`gy` นี้ทำงาน **เฉพาะตอนเคอร์เซอร์อยู่ในต้นไม้** เท่านั้น — ถ้าอยากคัดลอก path ตอนกำลังแก้ไฟล์ (นอกต้นไม้) ให้ใช้ `<Space>c…` แทน (ดู §7.3)

**ตัวอย่างการสร้างไฟล์ใหม่:**

```
1. <Space>e            -> เปิดต้นไม้
2. j/k เลื่อนไปที่โฟลเดอร์ lua/  แล้วกด <CR> หรือ o เพื่อกางออก
3. a                   -> ระบบถามชื่อไฟล์
4. พิมพ์  helpers.lua  แล้ว <CR>
   -> ได้ไฟล์ lua/helpers.lua และ selection ในทรีจะไปอยู่ที่ไฟล์ใหม่
   -> จากนั้นกด <CR> หรือ o เพื่อเปิดแก้
```

> เคล็ด: ถ้าอยากสร้างเป็น **โฟลเดอร์** ให้พิมพ์ชื่อลงท้ายด้วย `/` เช่น `utils/` หรือสร้างไฟล์พร้อมโฟลเดอร์ในทีเดียวได้เลยเช่น `lua/utils/init.lua` มันจะสร้างโฟลเดอร์ที่ยังไม่มีให้อัตโนมัติ

**เกร็ดสำคัญ `sync_root_with_cwd`:** คอนฟิกนี้เปิดออปชันนี้ไว้ แปลว่า "รากของต้นไม้" (root) จะวิ่งตาม cwd เสมอ พูดง่ายๆ คือเวลาเราสลับโปรเจกต์ด้วย project.nvim (ซึ่งจะเปลี่ยน cwd ให้) ต้นไม้จะ **re-root ไปที่โปรเจกต์ใหม่ให้เองอัตโนมัติ** ไม่ต้องปิดเปิดใหม่ นี่คือเหตุผลว่าทำไมทั้ง 4 ตัวถึงทำงานเข้าขากันได้ดี

---

### 4.2 telescope — เครื่องหาไฟล์/ข้อความแบบ fuzzy

ถ้า nvim-tree คือ "เดินดูทีละโฟลเดอร์" telescope คือ "พิมพ์ชื่อแล้วเด้งไปเลย" ซึ่งเร็วกว่ามากเมื่อเรารู้คร่าวๆ ว่าจะหาอะไร มันคือเครื่องมือที่คุณจะใช้บ่อยที่สุดในชีวิตประจำวัน

| Keymap | ทำอะไร |
|--------|--------|
| `<C-p>` | หาไฟล์ที่มองเห็นและไม่ถูก ignore (`find_files` — รวม untracked ที่ไม่ถูก ignore แต่จะข้ามไฟล์ที่อยู่ใน `.gitignore` และไฟล์ซ่อน) |
| `<Space>f` | หาไฟล์แบบ **git-aware** — ถ้าอยู่ใน git repo จะใช้ `git_files` (โชว์เฉพาะไฟล์ที่ git รู้จัก เร็วและสะอาด ไม่มี `node_modules`); ถ้าไม่ได้อยู่ใน repo จะ fallback เป็น `find_files` ให้เอง |
| `<Space>ps` | ค้นข้อความในไฟล์ขอบเขตเดียวกัน (live grep ผ่าน rg) — พิมพ์คำ แล้วมันไล่หาให้แบบสดๆ (เคารพ `.gitignore` เหมือน `find_files`) |

**ความต่างของ `<C-p>` กับ `<Space>f`:** ทั้งคู่กรองพวก build artifacts / dependencies ที่อยู่ใน `.gitignore` ออกให้เหมือนกัน (ทั้งคู่ **ไม่** เห็นไฟล์ที่ถูก ignore เช่น `.env` หรือไฟล์ซ่อน) ต่างกันตรงที่ `<Space>f` ถ้าอยู่ใน git repo จะใช้ `git_files` โชว์เฉพาะไฟล์ที่ git รู้จัก (tracked) ส่วน `<C-p>` (`find_files`) จะรวม untracked ที่ไม่ถูก ignore มาให้ด้วย วันปกติแนะนำให้ใช้ `<Space>f` เป็นหลัก แล้วสลับไป `<C-p>` เมื่ออยากเห็นไฟล์ใหม่ที่ยังไม่ได้ `git add` (แต่ยังไม่ถูก ignore)

**ปุ่มข้างใน picker (ตัวเลือกที่เด้งขึ้นมา):**

| ปุ่ม | ทำอะไร |
|------|--------|
| พิมพ์ตัวอักษร | กรองแบบ fuzzy — พิมพ์ `keymap` เจอ `keymaps.lua` ได้แม้ตัวไม่ติดกัน |
| `<C-n>` / `<C-p>` | เลื่อนเลือกรายการ ลง/ขึ้น (หรือใช้ลูกศร `Down`/`Up` ก็ได้) |
| `<CR>` | เปิดไฟล์ที่เลือกในหน้าต่างปัจจุบัน |
| `<C-v>` | เปิดไฟล์ในหน้าต่างแยกแนวตั้ง (vertical split) |
| `<C-x>` | เปิดไฟล์ในหน้าต่างแยกแนวนอน (horizontal split) |
| `<Esc>` | จาก insert mode กด `<Esc>` หนึ่งครั้ง = เข้า normal mode ในตัว picker (เลื่อนด้วย `j`/`k` ได้); กด `<Esc>` อีกครั้งจาก normal mode = ปิด picker |
| `<C-c>` | ปิด picker ทันทีจาก insert mode (ไม่ต้องผ่าน normal mode) |

> จุดที่มือใหม่มักงง: พอเปิด picker มา เคอร์เซอร์อยู่ใน **insert mode** (พิมพ์กรองได้เลย) กด `<Esc>` ครั้งแรกจะยัง **ไม่ปิด** แต่สลับไป normal mode ก่อน ถ้าจะปิดเร็วๆ ให้ใช้ `<C-c>` จะจบในทีเดียว

**ตัวอย่าง workflow หาไฟล์:**

```
1. <Space>f            -> เปิด picker หาไฟล์
2. พิมพ์  optio        -> รายการหด เหลือ options.lua
3. <CR>                -> เปิด lua/core/options.lua
```

**ตัวอย่าง live grep (หาข้อความ):** สมมติจำได้ว่ามีคำว่า `hlsearch` อยู่ที่ไหนสักที่ในคอนฟิก แต่จำไม่ได้ว่าไฟล์ไหน

```
1. <Space>ps           -> เปิด live grep
2. พิมพ์  hlsearch      -> โชว์ทุกบรรทัดในทุกไฟล์ที่มีคำนี้ พร้อม preview
3. <C-n>/<C-p> เลือกบรรทัดที่ต้องการ
4. <CR>                -> กระโดดไปที่บรรทัดนั้นในไฟล์นั้นเลย
```

live grep นี่แหละคือ superpower ที่ทำให้คุณไม่ต้องจำว่าอะไรอยู่ไฟล์ไหน แค่จำ "คำ" ก็พอ

---

### 4.3 grapple — คั่นหน้าไฟล์สำคัญ (curated shortlist)

ปัญหาของ telescope คือทุกครั้งที่จะข้ามไฟล์ ต้องพิมพ์หาใหม่เสมอ grapple แก้ปัญหานี้ด้วยแนวคิด "รายชื่อไฟล์สำคัญที่เราเลือกเอง" — คิดเหมือนการปัก **bookmark** ไฟล์ที่กำลังทำงานอยู่ 3–5 ไฟล์ แล้วกระโดดไปมาได้ทันที ต่างจากรายการ buffer ทั้งหมดตรงที่มัน **สั้น สะอาด และเราคุมเอง**

| Keymap | ทำอะไร |
|--------|--------|
| `<Space>m` | tag/untag ไฟล์ปัจจุบัน (กดครั้งแรก = ปักไว้, กดซ้ำ = เอาออก) |
| `<Space>M` | เปิดเมนูรายการไฟล์ที่ tag ไว้ (เลือกด้วย `j`/`k` แล้ว `<CR>` เพื่อกระโดดไป) |

นอกจากนี้ยังมีคำสั่งที่พิมพ์เองได้ เหมาะเอาไปผูก workflow เร็วๆ:

```
:Grapple select index=1     -> กระโดดไปไฟล์ที่ tag ลำดับ 1 ทันที
:Grapple select index=2     -> กระโดดไปไฟล์ลำดับ 2
:Grapple cycle_tags next    -> วนไปไฟล์ถัดไปในรายการ
```

> เกร็ดจากคอนฟิก: grapple ตั้ง `scope = "git"` ไว้ แปลว่ารายการ tag จะ **แยกตาม git repository** — โปรเจกต์ A มี tag ชุดของมัน, โปรเจกต์ B ก็มีชุดของมันเอง ไม่ปนกัน พอกลับมาโปรเจกต์เดิม tag เดิมก็ยังอยู่ครบ (ถ้าไม่ได้อยู่ใน repo มันจะ fallback ไปใช้ cwd แทน)

**ตัวอย่าง:** กำลังแก้ feature ที่ต้องสลับไปมาระหว่าง `keymaps.lua` กับ `options.lua`

```
1. เปิด keymaps.lua  ->  <Space>m   (ปักเป็น tag 1)
2. เปิด options.lua  ->  <Space>m   (ปักเป็น tag 2)
3. ทีนี้ไม่ว่าอยู่ไฟล์ไหน:
   :Grapple select index=1   -> เด้งกลับ keymaps.lua
   :Grapple select index=2   -> เด้งไป options.lua
   หรือกด <Space>M เปิดเมนูเลือกด้วยสายตา
```

พอชินแล้วจะติดใจมาก เพราะการข้ามไฟล์กลายเป็นแค่ปุ่มเดียว ไม่ต้องพิมพ์หาซ้ำ

---

### 4.4 project.nvim — สลับโปรเจกต์ + auto-cd

ตัวสุดท้ายเป็นตัวที่ทำงานเงียบๆ อยู่เบื้องหลังแต่สำคัญมาก มันทำสองอย่าง:

**1) Auto-cd อัตโนมัติ:** ทุกครั้งที่เปิดไฟล์ project.nvim จะไล่หา "รากของโปรเจกต์" ขึ้นไปเรื่อยๆ โดยดูจากไฟล์/โฟลเดอร์บ่งชี้อย่าง `.git`, `Makefile`, `package.json`, `Cargo.toml`, `go.mod`, `pyproject.toml`, `CMakeLists.txt` แล้ว **เปลี่ยน cwd (global) ไปที่รากนั้นให้เอง** ผลพลอยได้คือ `<Space>f`, telescope, `:terminal` และ nvim-tree (เพราะ `sync_root_with_cwd`) ทั้งหมดวิ่งตามโปรเจกต์ที่ถูกต้องโดยที่เราไม่ต้อง `cd` เอง

**2) Picker สลับโปรเจกต์:**

| Keymap | ทำอะไร |
|--------|--------|
| `<Space>pp` | เปิด picker รายการโปรเจกต์ที่เคยเปิด (recent projects) |

> หมายเหตุ: รายการนี้ **เริ่มจากว่างเปล่า** แล้วค่อยๆ เติมเองเมื่อเราไปเยือนโปรเจกต์ต่างๆ ดังนั้นวันแรกที่ลองกด `<Space>pp` อาจยังไม่มีอะไร ไม่ต้องตกใจ — เปิดไฟล์ในโปรเจกต์สัก 2–3 อันก่อน แล้วรายการจะโผล่มาเอง

**ตัวอย่างสลับโปรเจกต์:**

```
1. <Space>pp           -> เปิดรายการโปรเจกต์ที่เคยทำ
2. พิมพ์กรอง แล้วเลือก my-api ด้วย Down/Up (หรือ j/k ใน normal mode)
   หมายเหตุ: ใน picker นี้ <C-n> ถูก override เป็น rename_project อย่ากดผิด
3. <CR>                -> มันจะเปลี่ยน cwd ไปที่โปรเจกต์นั้น แล้วเปิดหน้าให้เลือกไฟล์ต่อ
                          (nvim-tree re-root ตาม, พร้อมทำงานต่อ)
```

---

### 4.5 ร้อยทุกอย่างเข้าด้วยกัน — workflow จริงตั้งแต่ต้นจนจบ

ทีนี้มาดูของจริงว่าเครื่องมือทั้ง 4 ทำงานเป็นทีมยังไง สมมติเริ่มเช้าวันใหม่ จะกลับมาแก้โปรเจกต์เดิม:

```
── เปิดโปรเจกต์ ─────────────────────────────
<Space>pp              เลือกโปรเจกต์  my-api  แล้ว <CR>
                       -> project.nvim cd เข้าราก, nvim-tree re-root ให้อัตโนมัติ

── สำรวจ/หาไฟล์ ────────────────────────────
<Space>e               เปิดต้นไม้ดูภาพรวมโครงสร้างสักครู่ แล้ว <Space>e ปิด
<Space>f               หาไฟล์  routes.py  -> <CR> เปิด
<Space>ps              พิมพ์  def login   หาว่าฟังก์ชัน login นิยามที่ไหน -> <CR> กระโดดไป

── ปักไฟล์หลักไว้กระโดด ─────────────────────
(อยู่ที่ routes.py)     <Space>m   ปักเป็น tag 1
<Space>f open models.py -> <Space>m   ปักเป็น tag 2
<Space>f open config.py -> <Space>m   ปักเป็น tag 3

── กระโดดไปมาระหว่าง 3 ไฟล์นี้ทั้งวัน ────────
<Space>M                       เปิดเมนู เลือกด้วยตา
:Grapple select index=1        เด้งไป routes.py ทันที
:Grapple select index=2        เด้งไป models.py
:Grapple cycle_tags next       วนไปไฟล์ถัดไปเรื่อยๆ
```

**สรุปวิธีเลือกเครื่องมือให้ถูกงาน:**

- ยังไม่รู้ว่าไฟล์อยู่ไหน / อยากเห็นภาพรวม / สร้าง-ลบ-เปลี่ยนชื่อไฟล์ -> **nvim-tree** (`<Space>e`)
- รู้ชื่อไฟล์คร่าวๆ -> **telescope find** (`<Space>f` หรือ `<C-p>`)
- รู้แค่ "คำ" ที่อยู่ในไฟล์ แต่ไม่รู้ไฟล์ไหน -> **telescope live grep** (`<Space>ps`)
- ไฟล์ 3–5 อันที่จะสลับไปมาทั้งวัน -> **grapple** (`<Space>m` ปัก, `<Space>M` เมนู)
- ข้ามไปทำอีกโปรเจกต์ -> **project.nvim** (`<Space>pp`)

จำไม่ต้องหมดในทีเดียวครับ เริ่มจาก `<Space>f` กับ `<Space>ps` ให้คล่องก่อน สองอันนี้จะเปลี่ยนชีวิตการเปิดไฟล์ของคุณไปเลย แล้วค่อยเพิ่ม grapple เข้ามาเมื่อเริ่มรู้สึกว่า "เอ๊ะ ไฟล์นี้เราสลับกลับมาบ่อยจัง" เดี๋ยวทุกอย่างจะค่อยๆ เข้าที่เอง

---

## 5. Plugins: เขียนโค้ด (LSP, Completion, Treesitter)

มาถึงหมวดที่ทำให้ Neovim ของเรากลายร่างจาก "โปรแกรมแก้ไขข้อความ" เป็น "IDE เต็มตัว" กันแล้วนะครับ ถ้าเคยใช้ VS Code แล้วชอบตรงที่มันเด้ง autocomplete ให้ กระโดดไปดู definition ของฟังก์ชันได้ หรือขีดเส้นแดงเตือนตอนพิมพ์ผิด — ทั้งหมดนั้นอยู่ในหมวดนี้ครับ และข่าวดีคือ config นี้ตั้งค่าให้พร้อมใช้แล้ว ไม่ต้องไปแตะอะไรเพิ่ม แค่เปิดไฟล์โค้ดขึ้นมาทุกอย่างก็ทำงานเอง

เราจะไล่ทีละชั้น: **LSP** (สมองที่เข้าใจโค้ด) → **blink.cmp** (เมนูเติมโค้ด) → **Treesitter** (สีและ indent) → **mini.pairs** (ปิดวงเล็บ) → **comment** (คอมเมนต์ในตัว) ค่อยๆ อ่านนะครับ ไม่ต้องรีบจำทุกปุ่ม จำที่ใช้บ่อยก่อนแล้วที่เหลือจะซึมเข้าหัวเอง

---

### 5.1 LSP คืออะไร และทำไมมันถึงเปลี่ยนชีวิต

**LSP (Language Server Protocol)** คือมาตรฐานกลางที่ให้ "ตัวเซิร์ฟเวอร์ภาษา" คุยกับ editor ได้ พูดง่ายๆ คือมีโปรแกรมแยกตัวหนึ่ง (เช่น `pyright` สำหรับ Python) ที่ _อ่านและเข้าใจ_ โค้ดของเราจริงๆ รู้ว่าตัวแปรนี้ถูกประกาศตรงไหน ฟังก์ชันนี้รับ argument อะไรบ้าง พิมพ์ผิดตรงไหน แล้ว Neovim ก็เอาข้อมูลนั้นมาแสดงให้เรา

config นี้ใช้ **native LSP ของ Neovim** (ตั้งแต่ 0.11+ Neovim มีระบบ LSP ในตัวที่ดีมากจนไม่ต้องพึ่ง plugin เสริมอย่าง lsp-zero อีกต่อไป) สิ่งที่ LSP ให้เราได้:

- **ไปที่ definition** — กระโดดไปดูว่าฟังก์ชัน/ตัวแปรถูกนิยามไว้ตรงไหน
- **hover docs** — เอาเคอร์เซอร์ไปวางแล้วดูคำอธิบาย/type ของสิ่งนั้น
- **rename ทั้งโปรเจกต์** — เปลี่ยนชื่อตัวแปรทีเดียว แก้ให้ทุกที่ที่ถูกอ้างถึง
- **code action** — แก้ปัญหาแบบอัตโนมัติ (import ที่ขาด, ลบตัวแปรไม่ใช้ ฯลฯ)
- **diagnostics** — เส้นเตือน error/warning พร้อมกระโดดไปทีละจุด
- **references** — หาว่าสิ่งนี้ถูกใช้ที่ไหนบ้าง
- **signature help** — บอกว่าฟังก์ชันที่กำลังพิมพ์รับ argument อะไร ตอนนี้อยู่ตัวที่เท่าไร

> **สำคัญ:** keymap ของ LSP เป็น *buffer-local* คือมันจะ "ติด" ก็ต่อเมื่อมี language server attach กับไฟล์นั้นแล้วเท่านั้น ถ้าเปิดไฟล์เปล่าๆ (เช่น `scratch.txt`) แล้วกด `gd` ไม่ทำงาน อย่าเพิ่งตกใจครับ นั่นเป็นเพราะไม่มี server สำหรับไฟล์ประเภทนั้น พอเปิด `.py` `.go` `.c` `.lua` ที่มี server รองรับ ปุ่มพวกนี้ก็จะมีชีวิตขึ้นมาเอง

#### Keymap หลักของ LSP

| ปุ่ม | โหมด | ทำอะไร |
|------|------|--------|
| `gd` | normal | ไปที่ **definition** ของสิ่งใต้เคอร์เซอร์ |
| `K` | normal | เปิด **hover** (คำอธิบาย/type) — กด `K` ซ้ำอีกครั้งเพื่อ *เข้าไปในหน้าต่างลอย* (เลื่อนอ่านได้) |
| `<Space>r` | normal | **rename** ตัวแปร/ฟังก์ชันทั้งโปรเจกต์ |
| `<Space>la` | normal | **code action** (quick fix / auto import ฯลฯ) |
| `[d` | normal | กระโดดไป **diagnostic ก่อนหน้า** (เปิด float ให้ด้วย) |
| `]d` | normal | กระโดดไป **diagnostic ถัดไป** (เปิด float ให้ด้วย) |
| `<Space>vd` | normal | ดู **diagnostic ของบรรทัดนี้** แบบละเอียด (float) |
| `<Space>lr` | normal | หา **references** — ที่ไหนบ้างที่ใช้สิ่งนี้ |
| `<Space>vws` | normal | ค้นหา **workspace symbol** — หาฟังก์ชัน/คลาสทั้งโปรเจกต์ด้วยชื่อ |
| `<C-h>` | **insert** | **signature help** — บอก argument ของฟังก์ชันที่กำลังพิมพ์ |

> เกร็ดเรื่อง `K`: กดครั้งแรกหน้าต่างลอยจะเด้งขึ้นมาแต่เคอร์เซอร์ยังอยู่ในโค้ด ถ้าเนื้อหาในหน้าต่างยาวจนอ่านไม่หมด กด `K` ซ้ำอีกทีเคอร์เซอร์จะ "กระโดดเข้าไป" ในหน้าต่างนั้น ทีนี้ก็เลื่อน `j`/`k` อ่านได้ เสร็จแล้วกด `q` เพื่อปิด

#### Keymap แถมจาก Neovim 0.11+ (ของแถมในตัว ไม่ต้องตั้งเอง)

Neovim เวอร์ชันใหม่ใส่ default keymap ชุดหนึ่งมาให้ทุกไฟล์ที่มี LSP โดยขึ้นต้นด้วย `g` (คิดซะว่า g = "go/get") มีทับซ้อนกับด้านบนบ้าง เก็บไว้เป็นทางเลือก:

| ปุ่ม | ทำอะไร |
|------|--------|
| `grn` | rename (เหมือน `<Space>r`) |
| `gra` | code action (เหมือน `<Space>la`) |
| `grr` | references (เหมือน `<Space>lr`) |
| `gri` | ไปที่ **implementation** |
| `gO` | **document symbols** — ดูโครงสร้าง symbol ในไฟล์นี้ (ฟังก์ชัน/คลาสทั้งหมด) |

จะใช้ชุด `<Space>...` หรือชุด `gr...` ก็ได้ตามถนัดครับ ผมแนะนำให้เลือกจำชุดเดียวก่อน แล้วอีกชุดค่อยเก็บทีหลัง

#### :Mason — คนดูแล server ให้เรา

server ภาษาต้อง "ติดตั้ง" ลงเครื่องก่อนถึงจะใช้ได้ config นี้ใช้ **Mason** เป็นตัวจัดการ ทั้ง `:Mason` และชุด LSP จะโหลดตอนเปิดไฟล์ (`BufReadPre`/`BufNewFile`) ดังนั้น **หลังจากเปิดหรือสร้างไฟล์โค้ดแล้ว** จึงจะใช้ `:Mason` ได้ พิมพ์ `:Mason` แล้ว Enter จะเปิดหน้าต่างที่เห็นรายการ server ทั้งหมด (ในนั้นกด `g?` ดู help ได้) และ config ตั้งไว้ให้ **auto-install** server เหล่านี้ให้อัตโนมัติหลังจากคุณเปิดไฟล์โค้ด (ไม่ใช่ตั้งแต่หน้า start screen ตอนเปิด Neovim เปล่าๆ):

| Server | ภาษา |
|--------|------|
| `pyright` | Python |
| `bashls` | Bash |
| `gopls` | Go |
| `clangd` | C / C++ |
| `lua_ls` | Lua (สำหรับแก้ config Neovim เอง) |

ครั้งแรกที่เปิดไฟล์โค้ดของภาษานั้นๆ อาจต้องรอสักครู่ให้มันโหลด server ลงมา ถ้าเปิดไฟล์ `.py` แล้วยังไม่มี diagnostic/hover ให้พิมพ์ `:Mason` เช็กว่า `pyright` ติดตั้งครบหรือยัง (มีเครื่องหมายถูกหน้าชื่อ) หรือพิมพ์ `:LspInfo` ดูว่ามี client attach อยู่ไหม

---

### 5.2 blink.cmp — เมนูเติมโค้ดอัตโนมัติ

พอ LSP รู้จักโค้ดแล้ว **blink.cmp** คือคนที่เอาความรู้นั้นมาโชว์เป็น "เมนูเติมโค้ด" (completion) ให้เราตอนพิมพ์ ไม่ต้องกดอะไรเรียก — แค่เริ่มพิมพ์ เมนูก็จะเด้งขึ้นมาเอง แนะนำตัวแปร ฟังก์ชัน คำสั่ง ให้ตามบริบท

| ปุ่ม | ทำอะไร |
|------|--------|
| `<C-n>` | เลื่อนเลือกรายการ **ถัดไป** (หรือใช้ `Down` ก็ได้) |
| `<C-p>` | เลื่อนเลือกรายการ **ก่อนหน้า** (หรือ `Up`) |
| `<CR>` (Enter) | **รับ** รายการที่เลือกอยู่ |
| `<C-Space>` | **เรียกเมนูขึ้นมาเอง** (เผื่อกรณีมันไม่เด้ง หรืออยากให้โชว์ documentation) |
| `<C-e>` | **ปิด/ยกเลิก** เมนู (พิมพ์ต่อตามที่เราพิมพ์เอง) |

blink.cmp ดึงข้อมูลมาจากหลายแหล่งพร้อมกัน: จาก **LSP** (ฉลาดที่สุด — รู้ type), จาก **path** (เติมชื่อไฟล์/โฟลเดอร์เวลาพิมพ์ path), จาก **snippets** (โครงโค้ดสำเร็จรูป ใช้ native `vim.snippet` ของ Neovim) และจาก **buffer** (คำที่เคยพิมพ์ในไฟล์)

> ตัวอย่างจริง: เปิดไฟล์ `.py` พิมพ์ `import o` เมนูจะเด้งเสนอ `os`, `operator` ฯลฯ กด `<C-n>` เลื่อนไปที่ `os` แล้วกด `<CR>` — ได้ `import os` ทันที หรือพิมพ์ชื่อ object แล้วตามด้วยจุด เช่น `os.` blink จะโชว์ method/attribute ทั้งหมดของ `os` ให้เลือก

> เกร็ด `<C-Space>`: terminal หลายตัว (Windows Terminal, iTerm2, Terminal.app) ส่งปุ่มนี้มาเป็นสัญญาณพิเศษ (`<Nul>`) config เลย map `<Nul>` ให้เรียกเมนูด้วย ดังนั้นถ้ากด `Ctrl+Space` แล้วเมนูไม่ขึ้น ไม่ใช่ config พังนะครับ มันถูกดักไว้ให้ทำงานได้แล้ว — **ยกเว้นบน macOS** ที่ระบบยึดปุ่มนี้ไปสลับภาษา ทำให้ไม่มีทางถึง Neovim เลย (ดูหัวข้อ 8.3)

---

### 5.3 nvim-treesitter — สีและ indent ที่แม่นขึ้นอัตโนมัติ

**Treesitter** คือระบบที่ _เข้าใจโครงสร้างไวยากรณ์_ ของโค้ดจริงๆ (parse เป็นต้นไม้ syntax) ต่างจากการไล่สีแบบเก่าที่แค่จับ pattern คร่าวๆ ผลลัพธ์คือ:

- **highlight ที่แม่นและละเอียดกว่า** — แยกสีได้ว่าอันไหน function อันไหน parameter อันไหน keyword จริงๆ
- **indent ที่ถูกต้องกว่า** — พอขึ้นบรรทัดใหม่ในบล็อกโค้ด เคอร์เซอร์เยื้องให้อัตโนมัติตรงตำแหน่งที่ควร

ข่าวดีคือ **มันทำงานเองล้วนๆ ไม่มี keymap ให้จำ** เปิดไฟล์แล้วสีสวยขึ้น indent ฉลาดขึ้นเอง config ตั้งให้ติดตั้ง parser ของภาษาที่ใช้บ่อยไว้แล้ว (lua, python, bash, go, c, cpp, rust, markdown, json, yaml, toml ฯลฯ) และคอมไพล์ให้อัตโนมัติในเบื้องหลังตอนเปิดครั้งแรก

> ถ้าเจอไฟล์ภาษาใหม่ที่ยังไม่มี parser พิมพ์ `:TSInstall <ชื่อภาษา>` เพิ่มได้ เช่น `:TSInstall typescript` แล้วเปิดไฟล์นั้นใหม่อีกครั้งสีก็จะมา

> **ข้อควรรู้:** config นี้ติดตั้ง **`nvim-treesitter-textobjects`** แล้ว → text object แบบ "ทั้งฟังก์ชัน/คลาส" (`vaf`/`vif`/`vac`/`vic`, `]f`/`[f`) ใช้ได้ในไฟล์โค้ด (ดู §3) และเพิ่ม **flash** (jump) + **mini.surround** (`gs`) ด้วย · ส่วน `mini.ai`, leap/hop **ไม่ได้ลง** (ไม่จำเป็น — built-in + treesitter-textobjects ครอบคลุมแล้ว)

---

### 5.4 mini.pairs — ปิดวงเล็บ/quote ให้อัตโนมัติ

เล็กแต่ช่วยได้เยอะครับ **mini.pairs** จะปิดคู่ให้อัตโนมัติเวลาพิมพ์ พิมพ์ `(` มันเติม `)` ให้ทันทีแล้ววางเคอร์เซอร์ไว้ตรงกลาง เหมือนกันกับ `[`, `{`, `"`, `'`, `` ` `` ไม่ต้องเสียเวลาพิมพ์ตัวปิดเอง

> ตัวอย่าง: พิมพ์ `print(` ในไฟล์ Python จะได้ `print()` โดยเคอร์เซอร์อยู่ในวงเล็บพอดี พิมพ์ข้อความต่อได้เลย และถ้าพิมพ์ `)` เอง มันจะ "ข้าม" ตัวปิดที่มีอยู่แทนที่จะเติมซ้อน (ไม่ได้ `))` เกินมา)

ไม่มี keymap ให้จำเช่นกัน — พิมพ์ตามปกติแล้วมันช่วยเอง

---

### 5.5 คอมเมนต์ (native — ในตัว Neovim 0.12 ไม่ต้องใช้ plugin)

Neovim 0.12 มีระบบคอมเมนต์ในตัวแล้ว ไม่ต้องพึ่ง plugin อย่าง Comment.nvim อีกต่อไป ใช้ผ่าน operator `gc` ซึ่งฉลาดพอที่จะรู้ว่าไฟล์นี้คอมเมนต์ด้วยอะไร (Python ใช้ `#`, C ใช้ `//`, Lua ใช้ `--` ฯลฯ)

| ปุ่ม | โหมด | ทำอะไร |
|------|------|--------|
| `gcc` | normal | toggle คอมเมนต์ **บรรทัดปัจจุบัน** |
| `gc{motion}` | normal | คอมเมนต์ตาม motion เช่น `gc3j` = 3 บรรทัดลงมา, `gcip` = ทั้งย่อหน้า |
| `gc` | **visual** | คอมเมนต์บรรทัดที่ select ไว้ |

> ตัวอย่างจริง: วางเคอร์เซอร์บนบรรทัด `x = 1` ในไฟล์ Python แล้วกด `gcc` → กลายเป็น `# x = 1` กด `gcc` ซ้ำ → กลับเป็น `x = 1` (toggle) หรือ select 5 บรรทัดด้วย `Vjjjj` แล้วกด `gc` → คอมเมนต์ทั้ง 5 บรรทัดทีเดียว

จำง่ายๆ ว่า `gc` = "go comment" ครับ

---

### 5.6 ลองใช้จริง: workflow เปิดไฟล์ Python แล้วแก้โค้ด

มาลองร้อยทุกอย่างเข้าด้วยกันด้วยตัวอย่างที่เจอจริงในชีวิตประจำวัน สมมติเปิดไฟล์ `app.py` ที่มีฟังก์ชันชื่อ `calculate_total` แล้วมีจุดที่เรียกใช้อยู่หลายที่

```
1. เปิดไฟล์:            :e app.py           (LSP pyright จะ attach ให้เอง รอสักครู่)
2. วางเคอร์เซอร์บนชื่อ    calculate_total
   ฟังก์ชันที่ถูกเรียก
3. กด gd                → กระโดดไปที่บรรทัดที่นิยาม calculate_total
4. กด K                 → เด้ง hover ดู signature + docstring
                          (กด K ซ้ำถ้าอยากเลื่อนอ่าน)
5. กด <Space>r          → พิมพ์ชื่อใหม่ เช่น compute_total แล้ว <CR>
                          → เปลี่ยนชื่อให้ทุกที่ในโปรเจกต์ทันที
6. กด ]d                → กระโดดไป error/warning ถัดไป (ถ้ามี)
7. อยู่บน error นั้น กด    <Space>la
                          → เมนู code action เด้ง (รายการมีหมายเลข) เช่น "add import"
                          พิมพ์หมายเลขของ action ที่ต้องการแล้วกด <CR> มันจะแก้ให้อัตโนมัติ
8. กด <C-s>             → บันทึกไฟล์
```

เห็นไหมครับว่าพอทั้งระบบทำงานด้วยกัน เราแทบไม่ต้องออกจาก editor เลย — หา, อ่าน, เปลี่ยนชื่อ, แก้ error, บันทึก จบในไม่กี่ปุ่ม นี่แหละคือความรู้สึกของการเขียนโค้ดใน Neovim ที่ตั้งค่าดีๆ

> **เคล็ดลับสำหรับมือใหม่:** ยังไม่ต้องจำทุกปุ่มในหมวดนี้ครับ เริ่มจาก 4 ตัวนี้ก่อน แล้วชีวิตจะดีขึ้นทันที — `gd` (ไปดู definition), `K` (อ่านว่านี่คืออะไร), `<Space>la` (ให้มันช่วยแก้), `<Space>r` (เปลี่ยนชื่อ) ที่เหลือค่อยๆ เก็บ และถ้าลืมว่ากดอะไร แตะ `<Space>` (กดครั้งเดียว) แล้วรอประมาณ 1 วินาที which-key จะโผล่เมนูมาเตือนให้เอง ไม่ต้องกลับมาเปิดคู่มือทุกครั้ง

---

## 6. Plugins: Git, Terminal และ Workflow จริง

มาถึงหมวดที่สนุกที่สุดแล้วครับ! หลังจากเราคล่องเรื่องการแก้ไขข้อความและกระโดดไปมาในไฟล์แล้ว คราวนี้เราจะพา Neovim ออกจาก "เอดิเตอร์เดี่ยวๆ" ไปสู่การเป็น **ศูนย์บัญชาการงานจริง** ที่คุณ commit โค้ด, รันคำสั่ง, และสำรวจ codebase ได้โดยแทบไม่ต้องละมือจากคีย์บอร์ดเลย

หัวใจของหมวดนี้คือ 3 อย่าง: **gitsigns** (เห็น git diff ริมบรรทัดแบบสดๆ), **lazygit** (UI git เต็มรูปแบบ), และ **toggleterm** (เทอร์มินัลในตัว) แล้วเราจะปิดท้ายด้วยการร้อยทุกอย่างเข้าด้วยกันเป็น workflow จริงที่คุณใช้ได้ตั้งแต่วันนี้

---

### 6.1 gitsigns — เห็นการเปลี่ยนแปลง git ทุกบรรทัดแบบเรียลไทม์

เวลาคุณแก้ไฟล์ในโปรเจกต์ที่เป็น git repo, gitsigns จะแสดง **สัญลักษณ์เล็กๆ ริมซ้ายสุด** (คอลัมน์ที่เรียกว่า "sign column" หรือ gutter) บอกว่าแต่ละบรรทัดเปลี่ยนไปยังไงเทียบกับเวอร์ชันที่ commit ไว้ล่าสุด

| สัญลักษณ์ | ความหมาย |
|-----------|----------|
| แถบ (มักเป็น `│`) สีเขียว | บรรทัด **ใหม่** ที่เพิ่มเข้ามา (added) |
| แถบสีเหลือง/ส้ม | บรรทัดที่ถูก **แก้ไข** (changed) |
| ขีดสามเหลี่ยม/`_` สีแดง | มีบรรทัด **ถูกลบ** ตรงจุดนี้ (deleted) |

**ทำไมถึงมีประโยชน์?** เพราะคุณจะเห็นด้วยตาทันทีว่า "ฉันแก้อะไรไปบ้างในไฟล์นี้" โดยไม่ต้องเปิด `git diff` เลย มันช่วยให้คุณ review งานตัวเองก่อน commit ได้ตลอดเวลา กลุ่มของบรรทัดที่เปลี่ยนติดๆ กันเราเรียกว่า **hunk** (ก้อนการเปลี่ยนแปลง) ซึ่งเป็นหน่วยที่เราจะทำงานด้วยต่อไป

#### กระโดดระหว่าง hunk ด้วย ]c / [c

แทนที่จะเลื่อนหาการเปลี่ยนแปลงเอง ให้กระโดดตรงไปเลย:

| Keymap | ทำอะไร |
|--------|--------|
| `]c` | กระโดดไป hunk **ถัดไป** |
| `[c` | กระโดดไป hunk **ก่อนหน้า** |

ตัวอย่าง: คุณเพิ่งกลับมาเปิดไฟล์ที่แก้ค้างไว้เมื่อวาน อยากดูว่าแก้ตรงไหนบ้าง ก็แค่กด `]c` รัวๆ เคอร์เซอร์จะเด้งไปหยุดที่การเปลี่ยนแปลงแต่ละจุดตามลำดับ พอถึงจุดที่อยากดูละเอียดก็ใช้คำสั่งในหัวข้อถัดไปได้เลย

> เกร็ด: keymap นี้ฉลาดกว่าที่เห็น ถ้าคุณอยู่ใน **diff mode** (เช่นตอนเปิด `<Space>hd` หรือ merge conflict) มันจะสลับไปใช้ `]c`/`[c` ดั้งเดิมของ Neovim ให้อัตโนมัติ เพื่อให้การเลื่อน diff ยังทำงานถูกต้อง

#### ทำอะไรกับ hunk ได้บ้าง (leader = Space)

เมื่อเคอร์เซอร์อยู่ **บน** hunk แล้ว (ไม่ต้องเลือก visual, แค่วางเคอร์เซอร์บนบรรทัดในก้อนนั้น) คุณสั่งงานมันได้ทันที:

| Keymap | ทำอะไร | ใช้เมื่อไหร่ |
|--------|--------|-------------|
| `<Space>hp` | **preview** hunk — เปิดหน้าต่างเล็กๆ โชว์ว่าเดิมเป็นยังไง แก้เป็นยังไง | อยากเช็กก่อนว่าแก้ถูกจุดไหม |
| `<Space>hs` | **stage** hunk — เพิ่มก้อนนี้เข้า staging area ของ git | พอใจกับการแก้ก้อนนี้แล้ว เตรียม commit |
| `<Space>hr` | **reset** hunk — คืนก้อนนี้กลับเป็นเวอร์ชันที่ commit ไว้ (ทิ้งการแก้) | แก้ผิด อยากย้อนเฉพาะก้อนนี้ |
| `<Space>hb` | **blame** บรรทัด — โชว์ว่าใคร commit บรรทัดนี้ เมื่อไหร่ พร้อมข้อความ commit เต็ม | สงสัยว่า "โค้ดบรรทัดนี้มาจากไหน ทำไมเขียนแบบนี้" |
| `<Space>hd` | **diff** ทั้งไฟล์ — เปิดมุมมองแบบ split เทียบไฟล์ปัจจุบันกับ index | อยากเห็นภาพรวมการแก้ทั้งไฟล์ |

> ข้อควรรู้เรื่องฐานเปรียบเทียบ: `<Space>hr` (reset), `<Space>hp` (preview) และ `<Space>hd` (diff) โดย default เทียบกับ **index (staging area)** ไม่ใช่ HEAD ถ้าอยากเทียบกับ commit ล่าสุดต้องตั้ง base เป็น HEAD เอง (เช่น `:Gitsigns change_base HEAD`)

จุดที่มือใหม่มักงงคือคำว่า **stage** ครับ ใน git ก่อนจะ commit เราต้อง "จัดของเข้ากล่อง" (staging area) ก่อน ข้อดีของ `<Space>hs` คือมัน stage ได้ **ทีละ hunk** ไม่ใช่ทั้งไฟล์ ทำให้คุณเลือก commit เฉพาะส่วนที่พร้อมได้ เช่น แก้บั๊กเสร็จแต่ยังมีโค้ด debug ค้างในไฟล์เดียวกัน ก็ stage เฉพาะ hunk ที่แก้บั๊ก แล้วเว้น hunk debug ไว้

**ตัวอย่างลำดับการทำงานจริงกับ gitsigns:**

```
]c            -> กระโดดไป hunk แรก
<Space>hp     -> ดู preview ว่าแก้อะไร (ถูกต้อง!)
<Space>hs     -> stage hunk นี้
]c            -> ไป hunk ถัดไป
<Space>hp     -> ดู preview (อ้าว อันนี้เผลอลบผิด)
<Space>hr     -> reset ทิ้งไป
]c            -> ไป hunk ถัดไป... ทำวนแบบนี้จนครบ
```

---

### 6.2 lazygit — UI git เต็มรูปแบบในหน้าจอเดียว

gitsigns เก่งเรื่องงานรายบรรทัด แต่พองานใหญ่ขึ้น เช่นอยากดู log ทั้งหมด, สลับ branch, แก้ commit message, หรือ push ขึ้น remote — เราใช้ **lazygit** ครับ มันคือ TUI (โปรแกรม git แบบ text UI ที่ควบคุมด้วยคีย์บอร์ด) ที่ทรงพลังมาก คิดง่ายๆ ว่า gitsigns = งานรายบรรทัด ส่วน lazygit = งานระดับ commit ขึ้นไป

เปิดด้วย:

| Keymap | ทำอะไร |
|--------|--------|
| `<Space>gg` | เปิด lazygit เต็มจอ |

พอเปิดมา หน้าจอจะแบ่งเป็น **5 พาเนล** เรียงจากบนลงล่างทางซ้าย แล้วมีช่องใหญ่ด้านขวาไว้โชว์ diff/รายละเอียดของสิ่งที่คุณเลือกอยู่ ปุ่มทั้งหมดต่อจากนี้เป็น **ปุ่มของ lazygit เอง ไม่ใช่ของ Neovim** นะครับ (เวอร์ชันอ้างอิงคือ lazygit v0.62.1 ปุ่ม default)

#### การนำทางพาเนล (5 พาเนล)

แต่ละพาเนลมีเลขกำกับ กดเลขกระโดดไปตรงพาเนลนั้นได้เลย:

| ปุ่ม | พาเนล | ไว้ทำอะไร |
|------|-------|-----------|
| `1` | Status | ภาพรวมสถานะ repo ปัจจุบัน |
| `2` | Files | ไฟล์ที่แก้/ยังไม่ stage — พาเนลที่ใช้บ่อยที่สุด |
| `3` | Local Branches | รายชื่อ branch (checkout / merge / rebase) |
| `4` | Commits | ประวัติ commit (ดู/แก้ประวัติ) |
| `5` | Stash | ของที่พักไว้ชั่วคราว |

การเลื่อน **ระหว่างพาเนล** กับการเลื่อน **ในลิสต์** เป็นคนละเรื่องกัน อย่าสับสน:

| ปุ่ม | ทำอะไร |
|------|--------|
| `Tab` / `→` / `l` | ไปพาเนล **ถัดไป** |
| `Shift+Tab` / `←` / `h` | ไปพาเนล **ก่อนหน้า** |
| `j` / `k` (หรือ ↑↓) | เลื่อนขึ้น-ลงในลิสต์ของพาเนลปัจจุบัน |
| `]` / `[` | สลับ **"แท็บย่อย"** ในพาเนล |

จุดที่มือใหม่พลาดบ่อย: `]`/`[` **ไม่ใช่** ปุ่มสลับพาเนล แต่มันสลับ "แท็บย่อย" ภายในพาเนลเดียวกัน เช่นพาเนล 3 (Local Branches) จริงๆ มีสามแท็บย่อยคือ Local / Remote / Tags กด `]`/`[` เพื่อวนดูทีละแท็บได้ ส่วนการย้ายพาเนลให้ใช้ `Tab`/ลูกศร/เลขเอา

#### พาเนล Files (`2`) — จัดของเข้ากล่องก่อน commit

นี่คือพาเนลที่คุณจะอยู่บ่อยที่สุด เพราะ "stage" คือขั้นตอนก่อน commit เสมอ:

| ปุ่ม | ทำอะไร |
|------|--------|
| `Space` | stage / unstage ไฟล์ที่เลือกอยู่ |
| `a` | stage / unstage **ทุกไฟล์** ทีเดียว |
| `Enter` | เข้าโหมดดู diff ของไฟล์ (staging view) เพื่อ stage **ทีละ hunk** |
| `d` | discard — ทิ้งการแก้ของไฟล์นี้ (ระวัง กู้ไม่ได้) |
| `e` | เปิดไฟล์นี้มาแก้ |
| `o` | เปิดไฟล์ด้วยโปรแกรมของระบบ |
| `s` | stash การแก้ทั้งหมด (พักไว้ก่อน) |

เรื่องละเอียดของ `Enter`: พอกดเข้า staging view แล้ว lazygit จะโชว์ diff ของไฟล์นั้นแยกเป็นก้อนๆ (hunk) กด `Space` เพื่อ stage **ทีละ hunk** ได้เลย ไม่ต้องเอาทั้งไฟล์ และถ้าอยากละเอียดยิ่งกว่านั้น กด `a` เพื่อสลับเป็น **line mode** (โหมดทีละบรรทัด) จากนั้นใช้ `j`/`k` เลื่อนแล้ว `Space` เลือก stage ทีละบรรทัดได้เลย เหมาะเวลามีทั้งโค้ดจริงกับโค้ด debug ปนกันในไฟล์เดียว

#### Commit — เขียน message แล้วบันทึก

| ปุ่ม | ทำอะไร |
|------|--------|
| `c` | commit — เด้ง UI ให้พิมพ์ message ในตัว lazygit แล้ว `Enter` ยืนยัน |
| `C` | commit โดยเปิด **editor ที่ตั้งไว้** (พิมพ์ message ในนั้น เซฟแล้วปิดเพื่อ commit) |
| `A` | **amend** commit ล่าสุดด้วยของที่ stage ไว้ |

เกร็ดตอนพิมพ์ message ผ่าน `c`: ถ้าอยากขึ้นบรรทัดใหม่ใน message (เขียน body ยาวๆ) อย่าเพิ่งกด `Enter` เฉยๆ เพราะมันจะยืนยัน commit ทันที ให้ใช้ `Ctrl+Enter` (หรือ `Ctrl+S`) เพื่อขึ้นบรรทัดใหม่แทน แล้วค่อย `Enter` ตอนจะยืนยันจริง

#### Sync — ส่งขึ้น/ดึงลง remote

| ปุ่ม | ทำอะไร |
|------|--------|
| `P` (Shift+p) | push ขึ้น remote |
| `p` | pull ลงมา |

ถ้า branch ปัจจุบันยังไม่มี upstream, lazygit จะถามให้เองว่าจะ push ไปที่ไหน ไม่ต้องจำคำสั่ง `--set-upstream` ให้ปวดหัว

#### พาเนล Local Branches (`3`) — สลับและจัดการ branch

| ปุ่ม | ทำอะไร |
|------|--------|
| `Space` | checkout — สลับไป branch ที่เลือก |
| `n` | สร้าง branch ใหม่ |
| `d` | ลบ branch ที่เลือก |
| `M` | merge branch ที่เลือก **เข้า** branch ปัจจุบัน |
| `r` | rebase branch ปัจจุบัน **ไปบน** branch ที่เลือก |
| `R` | เปลี่ยนชื่อ branch |

#### พาเนล Commits (`4`) — แก้ประวัติ commit

พาเนลนี้พลังเยอะ เพราะแก้ประวัติได้เลย เลือก commit ในลิสต์แล้วสั่งได้เลย:

| ปุ่ม | ทำอะไร |
|------|--------|
| `Enter` | ดูไฟล์ที่เปลี่ยนใน commit นี้ |
| `r` | reword — แก้เฉพาะ message |
| `s` | squash — รวม commit นี้กับ commit **ล่างถัดไป** (เก็บ message) |
| `f` | fixup — รวมแบบ**ทิ้ง** message ของ commit นี้ |
| `d` | drop — ลบ commit นี้ทิ้ง |
| `e` | เริ่ม interactive rebase จาก commit นี้ |
| `C` (Shift+c) | copy commit เพื่อ cherry-pick |
| `V` | paste (วาง) commit ที่ copy ไว้ |

> ⚠️ **คำเตือนสำคัญ:** การแก้ประวัติ commit (reword / squash / fixup / drop / rebase) กับ commit ที่ **push ขึ้น remote ไปแล้ว** เป็นอันตราย เพราะประวัติจะไม่ตรงกับ remote คุณต้อง force-push และมันจะกระทบคนอื่นที่ใช้ branch เดียวกัน (ประวัติเขาพังตาม) — กฎง่ายๆ คือ **แก้ประวัติเฉพาะ commit ที่ยังไม่ push เท่านั้น**

#### ปุ่มทั่วไป (Global) — จำปุ่มเดียวก็รอด

| ปุ่ม | ทำอะไร |
|------|--------|
| `?` | เมนู **help** ที่โชว์ปุ่มตาม context ที่คุณอยู่ตอนนั้น |
| `q` (หรือ `Ctrl+C`) | ออกจาก lazygit กลับสู่ Neovim |
| `Esc` | ยกเลิก / ถอยกลับหนึ่งก้าว |
| `/` | ค้นหา / กรองรายการ |
| `+` / `_` | สลับโหมดจอ **ถัดไป** / **ก่อนหน้า** (ย่อ-ขยายพาเนล) |
| `z` / `Z` | undo / redo (ผ่าน reflog) |

ปุ่มที่สำคัญที่สุดคือ **`?`** ครับ — ถ้าจะจำแค่ปุ่มเดียวจากทั้งหมดนี้ ให้จำปุ่มนี้ เพราะ `?` จะโชว์เฉพาะปุ่มที่ใช้ได้ในพาเนล/สถานการณ์ที่คุณอยู่ตอนนั้นพอดี ไม่ต้องท่องตารางทั้งหน้า เดี๋ยว lazygit บอกที่เหลือให้เอง ส่วน `z`/`Z` (undo/redo) ทำงานผ่าน reflog จึงครอบคลุมงานระดับ commit/branch (เช่นเผลอ drop หรือ merge ผิด) **ไม่ใช่** การย้อนการแก้เนื้อไฟล์นะครับ

#### Beginner workflow ตัวอย่างจริง

ถ้ายังไม่รู้จะเริ่มยังไง จำแค่สายพานนี้ก็ commit + push ได้ครบวงจรแล้ว:

```
<Space>gg     -> เปิด lazygit
2             -> ไปพาเนล Files
Space         -> stage ไฟล์ที่ต้องการ (หรือ a เพื่อ stage ทั้งหมด)
c             -> commit แล้วพิมพ์ message
Enter         -> ยืนยัน commit
P             -> push ขึ้น remote
q             -> ออกจาก lazygit กลับสู่ Neovim
```

> เกร็ด: จำปุ่มเดียวก็พอ — กด `?` ในแต่ละพาเนลเพื่อดูปุ่มที่ใช้ได้ตรงนั้น

**ทำไมต้องมีทั้ง gitsigns และ lazygit?** เพราะมันเสริมกัน — gitsigns ไว้ทำงานเร็วๆ ระหว่างแก้โค้ด (stage ทีละ hunk, preview, blame) ส่วน lazygit ไว้ทำงานระดับ commit ขึ้นไป (เขียน message, push, merge, จัดการ branch) พอ commit เสร็จใน lazygit แล้วกด `q` ออกมา คุณจะเห็นว่าสัญลักษณ์ริมบรรทัดของ gitsigns หายไป (เพราะการเปลี่ยนแปลงถูก commit ไปแล้ว) — สองตัวนี้ทำงานประสานกันอย่างลื่นไหล

---

### 6.3 toggleterm — เทอร์มินัลในตัว ไม่ต้องสลับหน้าต่าง

หลายครั้งเราต้องรันคำสั่งระหว่างเขียนโค้ด เช่น `python a.py`, `npm test`, `git status` การสลับไปหน้าต่าง terminal อื่นแล้วกลับมาทำให้เสียจังหวะ toggleterm เปิดเทอร์มินัลซ้อนใน Neovim ให้เลย บน Windows config นี้ตั้งค่าให้ใช้ **PowerShell** (pwsh ถ้ามี ไม่งั้น powershell) ให้อัตโนมัติ ส่วนบน macOS และ Linux ไม่ตั้ง — ใช้ `$SHELL` เดิมของคุณ (macOS default คือ zsh)

| Keymap | ทำอะไร |
|--------|--------|
| `<A-w>` | เปิด/ปิดเทอร์มินัล **แนวนอน** (แปะด้านล่างจอ) |
| `<A-q>` | เปิด/ปิดเทอร์มินัลแบบ **ลอย** (float — หน้าต่างลอยกลางจอ) |

(`<A-w>` = กด Alt ค้างแล้วกด w) keymap ทั้งสองเป็นแบบ toggle คือกดอีกทีเพื่อซ่อน แต่ **process ไม่ตาย** — คำสั่งที่รันค้างอยู่ยังทำงานต่อ ซ่อนไว้เฉยๆ กดกลับมาเมื่อไหร่ก็เห็นสถานะล่าสุด

#### เรื่องสำคัญที่สุด: การออกจาก terminal-insert mode

พอคุณคลิก/เข้าไปในเทอร์มินัล มันจะอยู่ใน **terminal mode** (คล้าย insert mode — พิมพ์อะไรลงไปตรงๆ ในเชลล์) ปัญหาคลาสสิกของมือใหม่คือ "กด Esc แล้วไม่ออก!" เพราะ Esc ในนี้ถูกส่งเข้าเชลล์ ไม่ใช่ให้ Neovim วิธีที่ถูกต้องคือ:

```
<C-\><C-n>   -> ออกจาก terminal-insert กลับสู่ normal mode ของ Neovim
```

(อ่านว่า: กด Ctrl ค้างแล้วกด `\`, จากนั้นกด Ctrl ค้างแล้วกด `n`) พอกลับมา normal mode แล้วคุณจะเลื่อนดู output ด้วย `hjkl`, ค้นหาด้วย `/`, หรือ yank ข้อความจากเทอร์มินัลได้เหมือนบัฟเฟอร์ปกติ อยากพิมพ์คำสั่งต่อก็กด `i` หรือ `a` เพื่อกลับเข้า terminal mode หรือจะกด `<A-w>`/`<A-q>` เพื่อซ่อนเทอร์มินัลไปเลยก็ได้

**ตัวอย่างการใช้จริง:**

```
<A-q>              -> เปิดเทอร์มินัลลอย
python a.py        -> รันสคริปต์ เห็น output ทันที
<C-\><C-n>         -> ออกมา normal mode
k k k              -> เลื่อนขึ้นไปอ่าน error ที่ output ยาวๆ
<A-q>              -> ซ่อนเทอร์มินัลกลับไปแก้โค้ด (process ยังอยู่)
```

---

### 6.4 Workflow จริง: ร้อยทุกอย่างเข้าด้วยกัน

ตอนนี้คุณมีเครื่องมือครบมือแล้ว มาดูกันว่าในสถานการณ์จริง เราใช้มันต่อกันเป็นสายพานยังไง นี่คือจุดที่ Neovim + config นี้เริ่ม "รู้สึกเหมือนซูเปอร์พาวเวอร์" จริงๆ

#### Workflow (ก): แก้บั๊กครบวงจร ตั้งแต่เปิดโปรเจกต์ถึง commit

สมมติมี bug report เข้ามาในโปรเจกต์ที่คุณเคยเปิดไว้แล้ว ลำดับการทำงานที่ลื่นที่สุดคือ:

```
1. <Space>pp        เปิด picker โปรเจกต์ล่าสุด -> เลือกโปรเจกต์ (cwd cd ไปที่ root ให้เอง)
2. <Space>f         หาไฟล์ที่น่าจะมีบั๊ก (git-aware) -> พิมพ์ชื่อไฟล์ -> <CR> เปิด
3. gd               วางเคอร์เซอร์บนฟังก์ชันที่สงสัย กด gd กระโดดไปนิยามของมัน
   K                กด K อ่าน hover ดู signature/เอกสารของฟังก์ชันนั้น
4. (แก้โค้ดตรงบั๊ก)  ใช้สกิล editing ที่เรียนมา เช่น ciw, dd, cc ฯลฯ
5. ]d               กระโดดไป diagnostic (error/warning) ถัดไปที่ LSP รายงาน
   <Space>vd        เปิด float ดูข้อความ error เต็มๆ ของบรรทัดนั้น
6. <Space>hp        preview hunk ที่เพิ่งแก้ ตรวจว่าถูกต้อง
   <Space>hs        stage hunk นั้น
7. <Space>gg        เปิด lazygit -> พิมพ์ commit message (c) -> push (P) -> q ออก
```

จุดที่ควรเข้าใจ: **`<Space>pp` กับ project.nvim ทำงานเงียบๆ เบื้องหลังให้ด้วย** — พอคุณเปิดไฟล์ในโปรเจกต์ มันจะ auto-cd ไปที่ root ของ repo อัตโนมัติ (มองหา `.git`, `package.json`, `pyproject.toml`, `go.mod` ฯลฯ) ทำให้ `<Space>f` และเทอร์มินัลของคุณเริ่มจากตำแหน่งที่ถูกต้องเสมอ ไม่ต้อง `cd` เอง

> `]d`/`<Space>vd`/`gd`/`K` เป็น keymap ของ LSP (ดูรายละเอียดในหมวด LSP) ในที่นี้เราแค่โชว์ว่ามัน **ร้อยเข้ากับ workflow git** ได้อย่างเป็นธรรมชาติ

#### Workflow (ข): สำรวจ codebase ที่ไม่เคยเห็นมาก่อน

เวลาเข้าโปรเจกต์ใหม่ที่ไม่รู้อะไรเลย เป้าหมายคือ "หาจุดตั้งต้น แล้วไล่ตามเส้นด้ายไปเรื่อยๆ" เครื่องมือที่ดีที่สุดคือ **live grep** (ค้นด้วยข้อความ) + `gd` + **grapple** (ปักหมุดไฟล์สำคัญ):

```
1. <Space>ps        live grep ทั้งโปรเจกต์ -> พิมพ์คำที่น่าจะเป็นจุดเริ่ม เช่น "def main"
                    หรือชื่อ error message ที่เห็น -> <CR> กระโดดไปบรรทัดนั้น
2. gd               บนชื่อฟังก์ชัน/ตัวแปร กด gd ไล่ไปหานิยาม เข้าใจว่ามันมาจากไหน
   <C-o>            กด Ctrl-o เพื่อ "ถอยกลับ" (jump back) มายังจุดเดิม -- ไล่เข้า-ออกได้เรื่อยๆ
3. <Space>m         เจอไฟล์สำคัญ! ปักหมุดด้วย grapple (tag) ไว้กลับมาทีหลังได้เร็ว
4. (ทำซ้ำข้อ 1-3)   ไล่ตามเส้นด้ายไปเรื่อยๆ ปัก grapple ไฟล์กุญแจสำคัญไว้ 3-4 ไฟล์
5. <Space>M         เปิดเมนู grapple ดู shortlist ไฟล์ที่ปักไว้ -> เด้งไปมาระหว่างไฟล์หลัก
```

ทำไมใช้ **grapple** ไม่ใช่บัฟเฟอร์ธรรมดา? เพราะพอสำรวจไปสักพัก คุณจะเปิดไฟล์เต็มไปหมดจนหาไม่เจอ grapple เป็น **shortlist ที่คุณคัดเอง** (curated) — เก็บเฉพาะ 3-5 ไฟล์กุญแจสำคัญของงานนี้ ทำให้ `<Space>M` เป็นเหมือน "แผงควบคุมส่วนตัว" ของโปรเจกต์ที่คุณกำลังทำ ต่างจากรายการบัฟเฟอร์ที่รกไปหมด (grapple ในนี้ตั้ง scope แบบ git ด้วย จึงจำ tag แยกต่อ repo — เปลี่ยนโปรเจกต์แล้ว shortlist ก็เปลี่ยนตาม)

> เกร็ด: อยากค้น "ทั้งข้อความ" ใช้ `<Space>ps` (live grep) แต่ถ้าจำ **ชื่อไฟล์** ได้อยู่แล้วให้ใช้ `<Space>f` (หรือ `<C-p>` เพื่อรวมไฟล์ untracked ที่ยังไม่ถูก ignore) — เลือกให้ถูกเครื่องมือจะเร็วขึ้นเยอะ

#### Workflow (ค): แก้เร็ว รันเทสต์ ไม่ละมือจากคีย์บอร์ด

งานเล็กๆ ที่ต้องแก้แล้วรันดูผลทันที เช่น แก้สคริปต์ Python:

```
1. <Space>f         เปิดไฟล์ -> แก้โค้ด
2. <C-s>            เซฟไฟล์ (กดได้ทั้งใน normal และ insert mode)
3. <A-w>            เปิดเทอร์มินัลแนวนอนด้านล่าง
   python a.py      รันดูผล
4. <C-\><C-n>       ออกจาก terminal-insert มาอ่าน output
5. <A-w>            ซ่อนเทอร์มินัล กลับไปแก้ต่อ (แก้แล้วเซฟ กด <A-w> รันซ้ำได้เลย)
6. <Space>hd        พอใจแล้ว ดู diff รวมทั้งไฟล์ก่อน -> <Space>gg commit
```

วงจร "แก้ → เซฟ → `<A-w>` รัน → ซ่อน → แก้" นี้คือหัวใจของการพัฒนาที่ไหลลื่น เพราะสายตากับมือคุณไม่เคยออกจากหน้าจอ Neovim เลย ลองฝึกจนมันกลายเป็นกล้ามเนื้อ แล้วคุณจะรู้สึกว่าเขียนโค้ดได้เร็วขึ้นแบบก้าวกระโดดครับ

---

**สรุปหมวดนี้:** gitsigns ให้คุณเห็นและจัดการการเปลี่ยนแปลงทีละ hunk (`]c`/`[c`, `<Space>hp/hs/hr/hb/hd`), lazygit (`<Space>gg`) ดูแลงาน git ระดับ commit/push/branch, toggleterm (`<A-w>`/`<A-q>`, ออกด้วย `<C-\><C-n>`) ให้เทอร์มินัลในตัว เมื่อนำมาร้อยกับ project.nvim, telescope, LSP และ grapple ที่เรียนมา คุณก็จะทำงานได้ครบวงจรตั้งแต่เปิดโปรเจกต์จนถึง push โดยแทบไม่ต้องออกจาก Neovim เลย นี่แหละครับคือความหมายของคำว่า "editor ที่กลายเป็น workflow"

---

## 7. เส้นทางสู่ Vim Expert + Cheatsheet

มาถึงหมวดนี้แปลว่าคุณเดินทางมาไกลมากแล้ว เก่งขึ้นเยอะเลย! หมวดนี้คือ "ของจริง" ที่แยกคนใช้ Vim เป็น กับคนใช้ Vim เก่ง ออกจากกัน เครื่องมือพวกนี้ไม่ได้อยู่ในปลั๊กอินไหนเป็นพิเศษ แต่เป็นความสามารถแกนกลางของ Vim ที่ติดตัวมาตั้งแต่แรก และมันทรงพลังมากจนหลายคนเลิกใช้ editor อื่นเพราะสิ่งเหล่านี้ ค่อยๆ ซึมซับทีละอย่างนะครับ ไม่ต้องรีบจำหมดในวันเดียว

### 7.1 dot `.` — ปุ่มที่ทรงพลังที่สุดใน Vim

`.` (จุด) คือคำสั่ง "ทำการแก้ไขครั้งล่าสุดซ้ำอีกครั้ง" ฟังดูธรรมดา แต่มันคือหัวใจของปรัชญา Vim ทั้งหมด ทุกครั้งที่คุณแก้อะไรสักอย่าง ให้ถามตัวเองว่า "ถ้าต้องทำแบบนี้อีก 10 ที่ ฉันจะออกแบบการแก้ครั้งนี้ยังไงให้กด `.` ซ้ำได้"

ตัวอย่างจริง สมมติมีคำว่า `apple` อยู่หลายที่ในไฟล์ แล้วอยากเปลี่ยนเป็น `orange` ทุกที่

```
วางเคอร์เซอร์ที่ apple แรก -> กด  ciw  -> พิมพ์ orange -> กด <Esc>
กด  n  (ถ้าใช้ search นำทาง) หรือเลื่อนไปที่ apple ตัวถัดไป
กด  .   -> apple ตัวนั้นกลายเป็น orange ทันที ไม่ต้องพิมพ์ใหม่
เลื่อนไปตัวถัดไป กด . อีก -> ซ้ำได้เรื่อยๆ
```

เคล็ดลับสำคัญ: การแก้ที่ "ทำซ้ำได้ดี" คือการแก้ที่จบในตัวเอง เช่น `ciw` (change inner word) ดีกว่าการกด `x` หลายทีแล้วพิมพ์ เพราะ `.` จะจำทั้งชุด "ลบคำ + พิมพ์ orange" ไว้เป็นก้อนเดียว พอเจอคำที่ยาวไม่เท่ากันก็ยังใช้ได้ เพราะ `iw` จับ "ทั้งคำ" ไม่ใช่ "จำนวนตัวอักษรที่แน่นอน"

### 7.2 Macros — อัดการกระทำเป็นชุดแล้วเล่นซ้ำ

ถ้า `.` ทำซ้ำได้แค่การแก้ครั้งเดียว macro คือการอัด "ลำดับหลายๆ ขั้นตอน" รวมทั้งการเคลื่อนที่ การพิมพ์ และคำสั่งต่างๆ แล้วเล่นซ้ำทั้งชุด

| ปุ่ม | ความหมาย |
|------|----------|
| `qa` | เริ่มอัด macro เก็บลง register `a` (ใช้ตัวอักษร a-z ตัวไหนก็ได้) |
| `q` | หยุดอัด (กด q อีกครั้งตอนอยู่ในโหมดอัด) |
| `@a` | เล่น macro ที่เก็บใน register `a` |
| `@@` | เล่น macro ตัวล่าสุดที่เพิ่งเล่นไปซ้ำ |
| `10@a` | เล่น macro `a` ซ้ำ 10 รอบรวด |

ตัวอย่างจริง สมมติมีรายการชื่อทีละบรรทัด อยากเติมเครื่องหมายคำพูดครอบและใส่ comma ท้าย เช่นเปลี่ยน `apple` เป็น `"apple",`

```
วางเคอร์เซอร์ที่ต้นบรรทัดแรก
กด  qa                 (เริ่มอัด ลง register a)
กด  I"<Esc>            (เติม " หน้าคำ แล้วออก insert)
กด  A",<Esc>           (เติม ", ท้ายบรรทัด)
กด  j                  (ลงบรรทัดถัดไป เตรียมพร้อมรอบต่อไป)
กด  q                  (หยุดอัด)
ตอนนี้เคอร์เซอร์อยู่บรรทัดที่ 2 แล้ว
กด  @a                 -> บรรทัดที่ 2 ถูกจัดรูปเหมือนกันเป๊ะ
กด  @@                 -> ทำบรรทัดถัดไปซ้ำ
หรือกด  10@a           -> ทำ 10 บรรทัดรวดเดียว
```

เคล็ดลับ: ให้ใส่การเลื่อนไปบรรทัด/ตำแหน่งถัดไป (เช่น `j`) ไว้ "ท้าย macro" เสมอ macro จะได้พร้อมทำงานต่อเนื่องได้ทันทีตอนเล่นซ้ำหลายรอบ ถ้าเผลอทำ macro ผิด กด `u` (undo) แล้วอัดใหม่ได้เลย ไม่ต้องเครียด

### 7.3 Registers — ที่เก็บข้อความหลายช่อง

หลายคนไม่รู้ว่า Vim มี "ที่เก็บ" (register) หลายช่อง ไม่ใช่แค่ clipboard ช่องเดียว การอ้าง register ทำได้ด้วยเครื่องหมาย `"` ตามด้วยชื่อ register แล้วค่อยตามด้วยคำสั่ง yank/paste

| การใช้ | ความหมาย |
|--------|----------|
| `"ayy` | yank บรรทัดปัจจุบันไปเก็บใน register ชื่อ `a` |
| `"ap` | paste จาก register `a` |
| `"0p` | paste จาก register `0` = ข้อความที่ **yank** ล่าสุด (ไม่ปนกับที่ลบ/เปลี่ยน) |
| `"+y` / `"+p` | ใช้ system clipboard ของเครื่อง (config นี้ผูกไว้ที่ `<Space>y`/`<Space>Y`/`<Space>P`) |
| `"/` | register ที่เก็บ **คำค้นล่าสุด** เช่นกด `"/p` วางคำที่เพิ่งค้น |
| `:reg` | เปิดดู register ทั้งหมดว่าช่องไหนมีอะไรอยู่ |

จุดที่มือใหม่มักงง: เวลาคุณ `x` ลบข้อความ แล้ว `p` มันจะเอาข้อความที่เพิ่งลบมาวาง ทำให้ข้อความที่ตั้งใจ yank ไว้หายไป วิธีแก้คือใช้ `"0p` ซึ่งดึงเฉพาะ "ของที่ yank" ล่าสุดจริงๆ ไม่ปนกับของที่ลบ

ตัวอย่างจริง:
```
"ayy   -> เก็บบรรทัดนี้ไว้ใน a (เหมือนพักไว้ในกระเป๋า)
ทำงานอื่นๆ ลบนู่นนี่ไปเรื่อยๆ
"ap    -> ดึงบรรทัดที่พักไว้ใน a ออกมาวาง ยังอยู่ครบ
```

ใน config นี้ `<Space>y`, `<Space>Y`, `<Space>P` คือทางลัดไปยัง register `+` (system clipboard) ที่เตรียมไว้ให้แล้ว จะได้ก๊อปข้ามไปวางในเบราว์เซอร์หรือโปรแกรมอื่นได้ (macOS ใช้ `pbcopy`/`pbpaste` ที่มีมาในตัว ไม่ต้องลงอะไร ส่วน Linux ต้องมี xclip/xsel หรือ wl-clipboard)

#### วางเข้า insert / command-line ด้วย `<C-r>`

เวลาอยู่ใน **insert mode** หรือ **command-line** (หลังกด `:` `/` `?`) การกด `p` **วางไม่ได้** — เพราะ `p` เป็นคำสั่งของ normal mode. ต้องใช้ **`<C-r>` ตามด้วยชื่อ register** เพื่อ "แทรก" ข้อความจาก register นั้นลงตรงเคอร์เซอร์

| การใช้ (ใน insert / command-line) | ความหมาย |
|-----------------------------------|----------|
| `<C-r>+` | วางจาก **system clipboard** (ที่ก๊อปมาจากเบราว์เซอร์/โปรแกรมอื่น) |
| `<C-r>"` | วางจาก register ไม่มีชื่อ (สิ่งที่เพิ่ง yank/ลบ) |
| `<C-r>0` | วางเฉพาะ **ของที่ yank ล่าสุด** (ไม่ปนกับของที่ลบ) |
| `<C-r>a` | วางจาก register `a` |

**ตัวอย่างที่เจอบ่อย:** มีคำสั่งยาวๆ ที่ก๊อปมา (เช่น `:set guifont=...`) → กด `:` แล้วกด **`<C-r>` ตามด้วย `+`** = วางทั้งบรรทัดลง command-line ได้เลย ไม่ต้องพิมพ์มือ. ปุ่ม `<C-r>` เป็นคำสั่งของ Vim เอง (ไม่พึ่ง paste ของ terminal) จึงกดได้เหมือนกันทุก terminal และทุก OS. **หมายเหตุตอน SSH:** `<C-r>"` / `<C-r>0` (register ในตัว nvim) ให้ผลเหมือนกันทุกที่ แต่ `<C-r>+` จะดึง clipboard ของ **เครื่องปลายทาง** (ต้องมี clipboard provider ที่เครื่องนั้น) ไม่ใช่ clipboard ของเครื่องคุณ

> ⚠️ **ระวังสับสน:** `<C-r>` เปลี่ยนความหมายตามโหมด — ใน **normal mode** คือ **redo** (ทำซ้ำสิ่งที่ undo) แต่ใน **insert / command-line** คือ **แทรกจาก register**. ปุ่มเดียวกัน คนละงาน ขึ้นกับโหมดที่อยู่

#### คัดลอก path ของไฟล์ที่เปิดอยู่ (`<Space>c…`)

ปุ่ม `y`/`Y`/`gy` ของ nvim-tree (คัดลอก ชื่อ/relative/absolute path) ทำงาน **เฉพาะตอนเคอร์เซอร์อยู่ในต้นไม้** เท่านั้น เวลาที่กำลัง **แก้ไฟล์อยู่ (นอกต้นไม้)** ให้ใช้ชุด `<Space>c…` แทน — คัดลอก path ของไฟล์ปัจจุบันเข้า system clipboard ได้จากทุก buffer:

| Keymap | คัดลอกอะไร | ตัวอย่าง (เปิด `lua/core/keymaps.lua`) |
|--------|-----------|---------|
| `<Space>cp` | relative path (เทียบกับ root ของโปรเจกต์) | `lua/core/keymaps.lua` |
| `<Space>cP` | absolute path | `~/.config/nvim/lua/core/keymaps.lua` |
| `<Space>cn` | ชื่อไฟล์อย่างเดียว | `keymaps.lua` |
| `<Space>cd` | โฟลเดอร์ (absolute) | `~/.config/nvim/lua/core` |

กด `<Space>c` แล้วหยุดค้าง → which-key เด้งกลุ่ม **"Copy path"** ให้เลือกต่อ. คัดลอกเสร็จมีข้อความ `Copied ...` แจ้งเตือน; ถ้าอยู่ใน buffer ที่ไม่ใช่ไฟล์ (ต้นไม้/terminal/ไฟล์ยังไม่ตั้งชื่อ) จะเตือนว่าไม่มี path และ **ไม่ล้าง clipboard ทิ้ง**

> เกร็ด: `<Space>cp` (relative) จริงๆ คือ "เทียบกับ cwd" — ซึ่ง project.nvim ตั้ง cwd = root ของโปรเจกต์ให้อยู่แล้ว เลยได้ path แบบ project-relative ตามที่อยากได้. ถ้าไฟล์อยู่ **นอกโปรเจกต์/คนละไดรฟ์** มันจะ fallback เป็น absolute ให้อัตโนมัติ. อยากได้ค่าคงที่ไม่ขึ้นกับ cwd → ใช้ `<Space>cP` (absolute)

### 7.4 Marks — บุ๊กมาร์กตำแหน่งในไฟล์

Mark คือการปักหมุดตำแหน่งไว้ แล้วกระโดดกลับมาได้ทันที เหมาะกับไฟล์ยาวๆ ที่ต้องสลับไปมาระหว่างสองจุด

| ปุ่ม | ความหมาย |
|------|----------|
| `ma` | ตั้ง mark ชื่อ `a` ที่ตำแหน่งเคอร์เซอร์ปัจจุบัน |
| `` `a `` | กระโดดไปตำแหน่ง **เป๊ะๆ** ของ mark `a` (ทั้งบรรทัดและคอลัมน์) |
| `'a` | กระโดดไป **ตัวอักษรแรกที่ไม่ใช่ช่องว่าง** ของบรรทัดที่ mark `a` ไว้ (ส่วน `` `a `` ไปตำแหน่งเป๊ะ) |
| `` `` `` | กระโดดกลับไปตำแหน่งก่อนหน้าที่เพิ่งกระโดดมา (สลับไปมาได้) |
| `:marks` | ดู mark ทั้งหมดที่ตั้งไว้ |

ตัวอย่างจริง: กำลังแก้ฟังก์ชันหนึ่งอยู่ แต่ต้องไปดูค่าคงที่ด้านบนสุดของไฟล์
```
ma           -> ปักหมุด a ตรงที่กำลังแก้อยู่
gg           -> เด้งขึ้นบนสุดไปดูค่าคงที่
`a           -> เด้งกลับมาที่เดิมเป๊ะ ทำงานต่อได้เลย
```
เกร็ด: `` ` `` (backtick) กับ `` `` `` (สอง backtick) มีประโยชน์มาก `` `` `` ให้คุณ "สลับ" ไปมาระหว่างสองจุดล่าสุดได้เหมือนปุ่ม toggle

### 7.5 Visual Block `<C-v>` — แก้หลายบรรทัดพร้อมกัน

Visual block (กด `<C-v>` = Ctrl+v) เลือกเป็น "คอลัมน์แนวตั้ง" แทนที่จะเลือกเป็นแถวแนวนอน ทำให้แก้หลายบรรทัดพร้อมกันในตำแหน่งเดียวกันได้

| ปุ่ม | ความหมาย |
|------|----------|
| `<C-v>` | เข้าโหมด visual block |
| `I` | แทรกข้อความ **หน้า** บล็อกที่เลือก (พอกด `<Esc>` จะไปทุกบรรทัด) |
| `A` | แทรกข้อความ **ท้าย** บล็อกที่เลือก |
| `gv` | เลือกพื้นที่ visual เดิมซ้ำอีกครั้ง (เผลอกดหลุดก็เรียกคืนได้) |
| `o` | สลับไปอยู่ปลายอีกด้านของ selection (ขยายจากอีกฝั่งได้) |

ตัวอย่างจริง อยากเติม `# ` (comment) หน้า 5 บรรทัดพร้อมกัน:
```
วางเคอร์เซอร์ต้นบรรทัดแรก
<C-v>        -> เข้า visual block
jjjj         -> เลือกลงมา 5 บรรทัด (ยังเป็นคอลัมน์แคบๆ)
I# <Esc>     -> พิมพ์ "# " แล้วออก
พอกด <Esc> ทั้ง 5 บรรทัดจะมี "# " นำหน้าพร้อมกัน
```
สำคัญ: ตอนพิมพ์ด้วย `I`/`A` คุณจะเห็นการเปลี่ยนแปลงแค่บรรทัดแรกก่อน ต้องกด `<Esc>` เท่านั้น ผลจึงจะกระจายไปทุกบรรทัด อย่าเพิ่งตกใจว่ามันไม่ทำงานนะครับ

### 7.6 คอมโบเด็ดที่ควรจำขึ้นใจ

พวกนี้คือชุดปุ่มสั้นๆ ที่เจอบ่อยจนควรเป็นกล้ามเนื้อ ลองฝึกทีละอันครับ

| คอมโบ | ทำอะไร | ตัวอย่าง |
|-------|--------|---------|
| `ciw` | change inner word — ลบทั้งคำแล้วเข้า insert | เคอร์เซอร์ที่ `foo` กด `ciw` -> `foo` หายไปพร้อมเข้า insert |
| `ct{char}` | change till — เปลี่ยนไปจนถึง (แต่ไม่รวม) ตัวอักษรนั้น | `name = "John"` เคอร์เซอร์ที่ต้น กด `ct"` -> ลบจนถึงก่อน `"` |
| `.` | ทำซ้ำการแก้ล่าสุด | หลัง `ciw`+พิมพ์ ก็กด `.` ที่คำอื่นได้ |
| `xp` | สลับตัวอักษร 2 ตัว | วางเคอร์เซอร์บนตัว `e` ใน `teh` แล้วกด `xp` -> ได้ `the` |
| `ddp` | สลับบรรทัดปัจจุบันกับบรรทัดล่าง | ลบบรรทัดนี้แล้ววางใต้บรรทัดถัดไป = สลับที่กัน |
| `>ip` | เพิ่ม indent ทั้งย่อหน้า (inner paragraph) | เคอร์เซอร์ในบล็อกโค้ด `>ip` -> เยื้องทั้งบล็อก |

### 7.7 แผนฝึกให้เก่งจริง

การเก่ง Vim ไม่ได้มาจากการอ่าน แต่มาจากการทำซ้ำจนเป็นนิสัย นี่คือแผนที่ยั่งยืนและไม่กดดันเกินไป

1. **เล่น `:VimBeGood`** — config นี้ติดตั้ง vim-be-good มาให้แล้ว (พิมพ์ `:VimBeGood` ใน normal mode) เป็นเกมฝึก motion แบบสนุกๆ วันละ 5-10 นาทีก็ช่วยได้มหาศาล
2. **ใช้ which-key ค้นปุ่ม** — เวลาลืมว่ากดอะไร แตะ `<Space>` (กดครั้งเดียว) แล้วรอประมาณ 1 วินาที popup จะโผล่มาบอกทุก leader mapping ไม่ต้องท่องจำ ให้เครื่องมือช่วยจำ
3. **เรียนสัปดาห์ละ 1-2 อย่าง** — อย่าโหมจำทั้งหมดในวันเดียว เลือกมา 1-2 เทคนิค (เช่นสัปดาห์นี้เอา macro กับ marks) แล้วบังคับตัวเองให้ใช้จนคล่อง สัปดาห์หน้าค่อยเพิ่ม
4. **เลิกใช้ arrow keys** — บังคับตัวเองให้เคลื่อนที่ด้วย `hjkl` และ motion (`w`, `b`, `f`, `/`) ช่วงแรกช้าและหงุดหงิด แต่หลังจากนั้นความเร็วจะพุ่งขึ้นอย่างที่ arrow keys ให้ไม่ได้ ถ้าใจแข็งพอ ลองแมป arrow keys ให้ไม่ทำงานชั่วคราวก็ได้

ใจเย็นๆ นะครับ ทุกคนที่ใช้ Vim คล่องวันนี้ เคยผ่านช่วงงงๆ ช้าๆ มาก่อนทั้งนั้น ความช้าตอนนี้คือการลงทุนเพื่อความเร็วในระยะยาว

---

### 7.8 CHEATSHEET — สรุป keymap ของ config นี้

#### ไฟล์ / นำทาง

| Keymap | ทำอะไร |
|--------|--------|
| `<Space>e` | เปิด/ปิด file tree (nvim-tree) |
| `<C-p>` | ค้นหาไฟล์ที่มองเห็นและไม่ถูก ignore รวม untracked (telescope find_files — เคารพ `.gitignore`) |
| `<Space>f` | ค้นไฟล์แบบรู้จัก git (git_files ถ้าอยู่ใน repo ไม่งั้น find_files) |
| `<Space>ps` | live grep — ค้นข้อความในไฟล์ขอบเขตเดียวกัน (เคารพ `.gitignore`) |
| `<Space>m` | tag/untag ไฟล์ปัจจุบันเข้า Grapple |
| `<Space>M` | เปิดเมนู Grapple (รายการไฟล์ที่คัดไว้) |
| `<Space>pp` | เปิด picker โปรเจกต์ล่าสุด (project.nvim) |
| ใน nvim-tree | `a` สร้าง, `d` ลบ, `r` เปลี่ยนชื่อ, `x` ตัด, `c` คัดลอก(ไฟล์), `p` วาง, `y`/`Y`/`gy` คัดลอก ชื่อ/relative/absolute path, `<CR>`/`o` เปิด, `H` toggle dotfiles, `R` refresh, `g?` help |

#### LSP (แก้โค้ด)

| Keymap | ทำอะไร |
|--------|--------|
| `gd` | ไปที่นิยาม (go to definition) |
| `K` | hover ดูข้อมูล (กด `K` ซ้ำเพื่อเข้า float) |
| `<Space>vd` | ดู diagnostics ของบรรทัด (float) |
| `[d` / `]d` | diagnostic ก่อนหน้า / ถัดไป |
| `<Space>la` | code action |
| `<Space>lr` | หา references |
| `<Space>r` | rename ตัวแปร/ฟังก์ชัน |
| `<Space>vws` | ค้น workspace symbol (ทั้งโปรเจกต์) |
| `<Space>vs` | document symbols ในไฟล์ (Telescope fuzzy + preview) |
| `<C-h>` (insert) | signature help |
| `grn` / `gra` / `grr` / `gri` / `gO` | (ดีฟอลต์ 0.11+) rename / code action / references / implementation / document symbols |

#### Git

| Keymap | ทำอะไร |
|--------|--------|
| `<Space>gg` | เปิด LazyGit (git TUI เต็มรูปแบบ) |
| `]c` / `[c` | ไป hunk ถัดไป / ก่อนหน้า |
| `<Space>hs` | stage hunk |
| `<Space>hr` | reset hunk |
| `<Space>hp` | preview hunk |
| `<Space>hb` | blame บรรทัดนี้ (แบบเต็ม) |
| `<Space>hd` | diff ไฟล์นี้ |

#### Terminal

| Keymap | ทำอะไร |
|--------|--------|
| `<A-w>` | terminal แนวนอน (`$SHELL` / PowerShell บน Windows) |
| `<A-q>` | terminal แบบ float |
| `<C-\><C-n>` | ออกจาก terminal-insert กลับสู่ normal mode |

#### Clipboard (system)

| Keymap | ทำอะไร |
|--------|--------|
| `<Space>y` | yank เข้า system clipboard (normal + visual) |
| `<Space>Y` | yank ทั้งบรรทัดเข้า clipboard |
| `<Space>P` | paste จาก system clipboard (normal + visual) |
| `<Space>cp` / `cP` | คัดลอก path ไฟล์ปัจจุบัน relative / absolute เข้า clipboard |
| `<Space>cn` / `cd` | คัดลอก ชื่อไฟล์ / โฟลเดอร์(absolute) เข้า clipboard |

#### Completion (blink.cmp)

| Keymap | ทำอะไร |
|--------|--------|
| `<C-n>` / `<C-p>` | เลื่อนเลือกรายการลง / ขึ้น |
| `<CR>` | ยอมรับรายการที่เลือก |
| `<C-Space>` | เปิดเมนู completion เอง |
| `<C-e>` | ยกเลิก/ปิดเมนู |

#### บันทึก / ค้นหา / เบ็ดเตล็ด

| Keymap | ทำอะไร |
|--------|--------|
| `<C-s>` / `<A-s>` | บันทึกไฟล์ (`<A-s>` = save แบบไม่ trigger autocmd) |
| `<Esc>` (Normal) | ล้าง highlight ของ search **และ** ปิดไฮไลต์ `f`/`t`/`F`/`T` ของ flash |
| `gcc` / `gc{motion}` | toggle comment บรรทัด / ตาม motion (native) |
| `J` / `K` (visual) | เลื่อนบรรทัดที่เลือกลง / ขึ้น |
| `:VimBeGood` | เปิดเกมฝึก Vim |

#### เสริมด้วย plugin (flash · function text object · surround)

| Keymap | ทำอะไร |
|--------|--------|
| `s` แล้วพิมพ์เป้าหมาย | **flash** กระโดดไปจุดที่พิมพ์ (พิมพ์จน label โผล่ แล้วกด label) · `S` = treesitter select |
| `f`/`t` (เสริม) | flash ทำให้ `f`/`t` ข้ามบรรทัดได้ + `;`/`,` วนต่อ · ไฮไลต์ค้างให้กด `;`/`,` ต่อ กด `<Esc>` เพื่อล้าง |
| `vif` / `vaf` · `vic` / `vac` | เลือก ใน/ทั้ง ฟังก์ชัน · คลาส (ไฟล์โค้ดจริง) |
| `dif` / `daf` · `]f` / `[f` | ลบไส้/ทั้งฟังก์ชัน · กระโดดฟังก์ชัน ถัดไป/ก่อนหน้า |
| `gsa`/`gsd`/`gsr` | surround เพิ่ม/ลบ/เปลี่ยนตัวคร่อม (เช่น `gsaiw"`, `gsd"`, `gsr"'`, `gsaiwt` ห่อ tag) · `gsf`/`gsF`/`gsh` หา/ไฮไลต์ |

#### Motion / Operator / Text object พื้นฐาน (built-in ล้วน)

Vim ใช้สูตร **operator + motion/text-object** เช่น `d` (delete) + `w` (word) = `dw` เข้าใจสูตรนี้แล้วจะผสมเองได้เป็นร้อยแบบ

| หมวด | ปุ่ม | ความหมาย |
|------|------|----------|
| Motion | `w` / `b` / `e` | ต้นคำถัดไป / ต้นคำก่อนหน้า / ท้ายคำ |
| Motion | `0` / `^` / `$` | ต้นบรรทัด / อักษรแรกที่ไม่ใช่ช่องว่าง / ท้ายบรรทัด |
| Motion | `gg` / `G` | บนสุด / ล่างสุดของไฟล์ |
| Motion | `f{char}` / `t{char}` | กระโดดถึง / กระโดดจนก่อนตัวอักษรนั้นในบรรทัด |
| Motion | `{` / `}` | ย่อหน้าก่อนหน้า / ถัดไป |
| Motion | `%` | กระโดดไปวงเล็บ/ปีกกาคู่ตรงข้าม |
| Operator | `d` / `c` / `y` | delete / change (ลบแล้วเข้า insert) / yank |
| Operator | `>` / `<` / `=` | เพิ่ม indent / ลด indent / จัด indent อัตโนมัติ |
| Text object | `iw` / `aw` | inner word / a word (รวมช่องว่าง) |
| Text object | `i"` / `a"` | ในเครื่องหมายคำพูด / รวมเครื่องหมายด้วย |
| Text object | `i(` `i[` `i{` | ในวงเล็บ/วงเล็บเหลี่ยม/ปีกกา (ใช้ `a` แทน `i` เพื่อรวมตัวคร่อม) |
| Text object | `ip` / `ap` | inner paragraph / a paragraph |
| Text object | `it` / `at` | ใน HTML/XML tag / รวม tag |

> หมายเหตุ: นอกจาก built-in ข้างบน config ติดตั้ง **nvim-treesitter-textobjects** แล้ว → `vif`/`vaf` (ฟังก์ชัน), `vic`/`vac` (คลาส), `]f`/`[f` ใช้ได้ในไฟล์โค้ด (ดู §3) · และ **mini.surround** (`gsa`/`gsd`/`gsr`) + **flash** (`s` jump) — ดูตาราง "เสริมด้วย plugin" ด้านบน

ตัวอย่างการผสม operator + text object เพื่อให้เห็นภาพ:
```
di(   -> ลบทุกอย่างในวงเล็บ ( ... )
ci"   -> ลบข้อความในเครื่องหมายคำพูดแล้วเข้า insert
ya{   -> yank ทั้งบล็อก { ... } รวมปีกกา
yi"   -> เคอร์เซอร์ที่ "hello" กด yi" -> คัดลอกคำว่า hello (ไม่รวม ")
das   -> ลบทั้งประโยค (a sentence)
```

จำสูตรไว้ครับ: **"ทำอะไร (operator) + กับอะไร (text object/motion)"** เมื่อคุณคิดเป็นประโยคแบบนี้ได้ คุณจะเลิกกด `x` ทีละตัว แล้วเริ่มแก้โค้ดด้วยความคิด ไม่ใช่ด้วยนิ้ว นั่นแหละคือจุดที่คุณกลายเป็น Vim expert อย่างแท้จริง สู้ๆ นะครับ!

---

## 8. แก้ปัญหาที่พบบ่อย + เคล็ดลับเฉพาะเครื่อง

มาถึงหมวดสุดท้ายแล้วครับ หมวดนี้คือ "กล่องเครื่องมือฉุกเฉิน" ที่เอาไว้เปิดตอนมีอะไรไม่เป็นไปตามที่หวัง อย่าเพิ่งตกใจหรือคิดว่า config พังนะครับ ปัญหาส่วนใหญ่บน Windows เกิดจากเรื่องเล็กๆ แบบ "ลืมปิด-เปิด terminal ใหม่หลังลงของ" หรือ "ยังไม่เปิดไฟล์เลยเลยยังไม่มีเมนู" ทั้งนั้น ผมจะเรียงให้เป็น **อาการ (symptom) → สาเหตุ → วิธีแก้** ค่อยๆ ไล่ทีละข้อ เดี๋ยวก็ผ่านครับ

> เคล็ดลับทองคำข้อเดียวที่อยากให้จำก่อนเลย: **ทุกครั้งที่ติดตั้งอะไรที่ไปแก้ PATH (เช่นลง compiler, ripgrep, lazygit) ต้องปิด terminal แล้วเปิดใหม่เสมอ** ไม่งั้น Neovim ที่รันอยู่จะยังมองไม่เห็นของที่เพิ่งลง แล้วคุณจะงงว่าลงแล้วทำไมยังไม่ขึ้น

---

### 8.1 Treesitter: highlight ไม่ขึ้น / คอมไพล์ parser ไม่ได้ ("program not found")

**อาการ:** เปิดไฟล์โค้ดแล้วสีไม่มา (เป็นสีเดียวจืดๆ) หรือตอนลง parser เจอ error ทำนอง `program not found` / คอมไพล์ไม่ผ่าน

**สาเหตุ:** nvim-treesitter (main branch) ต้อง **คอมไพล์ parser เอง** ซึ่งต้องมีเครื่องมือครบบน PATH ถ้าขาดตัวใดตัวหนึ่งก็คอมไพล์ไม่ได้ สิ่งที่ต้องมีบน PATH คือ:

- `tree-sitter` CLI (เวอร์ชัน **>= 0.26.1**) — เวอร์ชันเก่ากว่านี้ nvim-treesitter branch `main` ไม่รับ เช็คด้วย `tree-sitter --version` เสมอ อย่าเชื่อชื่อแพ็กเกจ

```powershell
# Windows
winget install -e --id tree-sitter.tree-sitter-cli
```

```bash
# macOS
brew install tree-sitter-cli
```

```bash
# Linux (Debian/Kali) — ถ้าเวอร์ชันจาก npm เก่าเกินไป ให้ใช้ไบนารีจาก release แทน
npm install -g tree-sitter-cli
tree-sitter --version
```

> **กับดักชื่อ formula บน Homebrew:** `tree-sitter` กับ `tree-sitter-cli` เป็นคนละ formula กัน
> ตัวที่ต้องการคือตัวที่ตอบ `tree-sitter --version` ได้ >= 0.26.1 เช็คเวอร์ชันเสมอ ไม่ใช่เช็คชื่อ

- `tar` และ `curl` (มีมาในตัวอยู่แล้วทั้ง Windows 11, macOS และ Linux)
- **C compiler**
  - **macOS:** `clang` จาก Xcode Command Line Tools — `xcode-select --install`
  - **Linux:** `gcc` จาก `build-essential`
  - **Windows:** `gcc` จาก msys2 หรือ `clang` จาก LLVM

config ตั้ง `vim.env.CC = "gcc"` ให้อัตโนมัติ **เฉพาะบน Windows และเฉพาะเมื่อคุณยังไม่ได้ตั้ง `$CC` เอง** (คือถ้าคุณตั้ง `$CC` ไว้แล้ว config จะเคารพของคุณ ไม่ทับให้) เพราะฉะนั้นถ้าลง gcc จาก msys2 มาก็มักใช้ได้เลย เหตุผลที่ต้องตั้งคือ tree-sitter CLI บน Windows จะไปหา MSVC (`cl`) เป็นค่า default แล้วไม่ยอมถอยไปใช้ gcc/clang เอง

> **บน macOS และ Linux config ไม่แตะ `$CC` เลย** เพราะ tree-sitter CLI ใช้ `cc` เป็น default อยู่แล้ว ซึ่งก็คือ clang (macOS) หรือ gcc (Linux) — **อย่าไปตั้ง `CC=gcc` เองบน Mac** `/usr/bin/gcc` เป็นแค่ shim ของ clang ไม่ได้อะไรเพิ่ม

> อย่าลืม: หลังลง tree-sitter หรือ gcc เสร็จ **ปิด-เปิด terminal ใหม่** แล้วค่อยเปิด nvim ไม่งั้น PATH ยังไม่อัปเดต

**วิธีเช็คว่ามีของครบไหม** — เปิด nvim แล้วรัน:

```vim
:checkhealth nvim-treesitter
:echo exepath('tree-sitter')
:echo exepath('cc')      " macOS/Linux — บน Windows ใช้ exepath('gcc')
```

ถ้า `exepath(...)` คืนค่าเป็นสตริงว่าง แปลว่ายังหาไม่เจอบน PATH (ย้อนกลับไปลง + ปิด-เปิด terminal)

**วิธีแก้ / ลง parser:**

```vim
:TSInstall <lang>     " ลง parser ของภาษานั้น เช่น :TSInstall python
:TSInstall! <lang>    " เติม ! เพื่อ reinstall (ลงใหม่ทับของเดิม เผื่อเจ๊ง)
:TSUpdate             " อัปเดต parser ทั้งหมดให้เป็นเวอร์ชันล่าสุด
```

**เกร็ดสุดท้าย:** การลง parser เป็นแบบ async (ทำงานเบื้องหลัง) บางทีลงเสร็จแล้วแต่ buffer ที่เปิดค้างไว้ก่อนหน้ายังไม่ highlight ให้ **เปิดไฟล์นั้นใหม่** หรือสั่ง:

```vim
:lua vim.treesitter.start()
```

แล้วสีจะเด้งขึ้นมาทันที

---

### 8.2 LSP / completion / `:Mason` ไม่ทำงาน

**อาการ:** พิมพ์ `:Mason` แล้วขึ้นว่าไม่รู้จักคำสั่ง, หรือ autocomplete ไม่เด้ง, หรือ `gd`/`K` ไม่ทำงาน

**สาเหตุ (สำคัญมาก อ่านให้ดี):** ใน config นี้ LSP + blink.cmp ถูกตั้งให้ **lazy-load** คือจะโหลดตอนเกิด event `BufReadPre` / `BufNewFile` ของไฟล์ **ใดก็ได้** (ไม่จำกัดว่าต้องเป็นไฟล์โค้ด) พูดง่ายๆ คือ **ถ้าคุณยังไม่เปิดไฟล์สักไฟล์เลย `:Mason` จะยังไม่มี** เพราะ config ไม่ได้ตั้ง cmd trigger ไว้

> วิธีแก้ง่ายที่สุด: **เปิดไฟล์อะไรสักไฟล์ก่อน** (เช่น `nvim README.md` หรือกด `<Space>e` แล้วเปิดไฟล์) แล้วค่อยพิมพ์ `:Mason` คราวนี้จะมีให้แน่นอน

**วิธีเช็ค:**

```vim
:checkhealth vim.lsp    " ควรเห็น active client เมื่อเปิด filetype ที่มี server และ server ลงเสร็จแล้ว
:checkhealth mason
:MasonLog               " ดู log ตอนติดตั้ง server ล้มเหลว (มีประโยชน์มากเวลา server ลงไม่ผ่าน)
```

**เกร็ดที่ต้องรู้:**

- การติดตั้ง server ครั้งแรกเป็นแบบ **async** — อาจต้องรอสักครู่ หรือปิดไฟล์เปิดใหม่ แล้ว client ถึงจะ attach
- server บางตัวมี **dependency** ที่ต้องลงเพิ่มเอง:
  - `pyright` / `bashls` ต้องมี **Node/npm**
  - `gopls` ต้องมี **Go**

ถ้า server ลงไม่ผ่านซ้ำๆ ให้เปิด `:MasonLog` อ่านดู มักจะฟ้องตรงๆ ว่าขาด dependency ตัวไหน

---

### 8.3 ปุ่มไม่ทำงาน (Ctrl+Space, Alt+...) โดยเฉพาะใน terminal และบน macOS

**อาการ:** กด `Ctrl+Space` เพื่อเปิดเมนู completion แล้วไม่มีอะไรเกิดขึ้น (เจอบ่อยเวลารันใน terminal)

**สาเหตุ:** terminal หลายตัวไม่ได้ส่ง `Ctrl+Space` ตรงๆ แต่ส่งมาเป็น `<Nul>` แทน — ซึ่ง config **map `<Nul>` ไว้ใน blink ให้แล้ว** ดังนั้นจริงๆ มันควรทำงาน แต่ถ้าไม่ทำงานก็ไม่ต้องกังวลเลย เพราะ **โดยปกติคุณไม่ต้องกด Ctrl+Space เอง** blink จะเด้งเมนูขึ้นมาให้อัตโนมัติตอนคุณพิมพ์อยู่แล้ว

**วิธีแก้ / วิธีสืบสวนว่าปุ่มส่ง code อะไรจริงๆ:**

- อยู่ใน **insert mode** กด `<C-v>` (Ctrl+v) แล้วกดปุ่มที่สงสัย → จะเห็น code จริงที่ terminal ส่งมา
- ดู mapping ปัจจุบันของ `<Nul>` ด้วย:

```vim
:verbose imap <Nul>
```

จะบอกว่า `<Nul>` ถูกผูกกับอะไรและมาจากไฟล์ไหน

#### เฉพาะ macOS: Ctrl+Space ถูกระบบยึดไป

macOS จองปุ่ม `Ctrl+Space` ไว้ให้ **"Select the previous input source"** (สลับภาษา)
ซึ่งระบบดักไว้ก่อนที่ terminal ตัวไหนจะได้เห็น — แปลว่า **ทั้ง `Ctrl+Space` และ `<Nul>`
ไม่มีทางถึง Neovim เลย** ทันทีที่คุณมี input source ตัวที่สอง (เช่นเพิ่มคีย์บอร์ดไทย)

ปิดได้ที่: เมนู Apple → System Settings… → Keyboard → Keyboard Shortcuts… → Input Sources

ถ้าไม่อยากปิด (เพราะใช้สลับไทย/อังกฤษอยู่) ก็ไม่เป็นไร — completion ยังเด้งอัตโนมัติ
ตอนพิมพ์เหมือนเดิม ที่หายไปคือการเรียกด้วยมือเท่านั้น หรือจะไป map ปุ่มอื่นให้ส่ง
`<Nul>` ในตัว terminal ก็ได้

#### เฉพาะ macOS: ปุ่ม `<A-...>` กับภาษาไทย

บน macOS ปุ่ม Option เป็นปุ่มผสมอักขระโดย default กด `<A-s>` / `<A-w>` / `<A-q>`
จะได้ `ß` / `∑` / `œ` ออกมาแทนที่จะทำงาน ต้องตั้ง terminal ให้ส่ง Meta ก่อน
(WezTerm ส่ง Esc+ จาก Option ซ้ายอยู่แล้วโดยไม่ต้องตั้งอะไร ส่วนตัวอื่นดูตารางใน
[INSTALL_MACOS_TH.md](INSTALL_MACOS_TH.md))

**แต่ถึงตั้งแล้วก็ยังมีข้อจำกัดที่แก้ไม่ได้:** ตอนที่ input source เป็น **ภาษาไทย**
Option ซ้าย+w จะส่งปุ่มตาม layout ไทย ไม่ใช่ `^[w` เพราะฉะนั้น `<A-w>`/`<A-q>`/`<A-s>`
**จะไม่ทำงานขณะพิมพ์ไทย** อันนี้เป็นพฤติกรรมของ input source ของ macOS เอง แก้ที่
config ไม่ได้

ทางออก: ใช้ **`<C-\>`** เปิด terminal แบบลอยแทน — map ไว้ในโหมด normal เพื่อการนี้
โดยเฉพาะ (`<C-\><C-n>` ยังใช้ออกจาก terminal mode ได้ตามปกติ เพราะ `<C-\>` เดี่ยว ๆ
map เฉพาะโหมด normal)

ถ้าใช้ Neovide บน macOS: config ตั้ง `vim.g.neovide_input_macos_option_key_is_meta = "only_left"`
ไว้ให้แล้วใน `lua/core/options.lua`

> **วิธีเช็คว่าเป็นปัญหาปุ่มจริงไหม:** ลองพิมพ์ `:ToggleTerm` ตรง ๆ ถ้าเปิดได้แปลว่า
> plugin ทำงานปกติ ปัญหาอยู่ที่ปุ่มอย่างเดียว

---

### 8.4 เปิด nvim แล้วจอว่าง / `<Space>f` ขึ้น error

**อาการที่ 1 — จอว่าง:** เปิด nvim มาแล้วเป็น buffer เปล่าๆ ไม่มี file tree

**สาเหตุ + วิธีแก้:**

- ถ้าคุณสั่ง `nvim <directory>` เช่น `nvim .` หรือ `nvim ~/proj` → config จะ `cd` เข้าโฟลเดอร์นั้นให้ แล้วเปิด nvim-tree ให้อัตโนมัติ
- แต่ถ้าคุณรันแค่ `nvim` เปล่าๆ (ไม่ตามด้วยอะไร) แล้วเจอ buffer ว่าง → **นี่คือพฤติกรรมปกติ ไม่ใช่ bug** เปิด tree เองได้ด้วย `<Space>e`

**อาการที่ 2 — `<Space>f` เคยชอบ error นอก git repo:**

`<Space>f` ตอนนี้เป็น **git-aware** แล้ว: ถ้าอยู่ใน git repo มันใช้ `git_files`, ถ้าอยู่ **นอก** repo มัน fallback ไปเป็น `find_files` ให้เอง (จึง **ไม่ error** อีกต่อไป)

> ถ้าอยากค้นไฟล์แบบไม่สนใจ git เสมอ (นอก repo ก็หาได้ทุกไฟล์) ให้ใช้ `<C-p>` แทน

---

### 8.5 ไอคอนเป็นสี่เหลี่ยม / ตัวประหลาด

**อาการ:** ใน file tree, statusline หรือเมนูต่างๆ ไอคอนขึ้นเป็นสี่เหลี่ยม □ หรือตัวอักษรประหลาดๆ แทนที่จะเป็นไอคอนสวยๆ

**สาเหตุ:** ไอคอนพวกนี้มาจาก **nvim-web-devicons** ซึ่งใช้ glyph พิเศษของ **Nerd Font** ถ้า terminal ของคุณไม่ได้ตั้งฟอนต์เป็น Nerd Font มันก็เรนเดอร์ glyph พวกนั้นไม่ได้

**วิธีแก้:** ไปตั้ง **Nerd Font** ใน profile ของ terminal ที่คุณใช้ แค่นี้ไอคอนก็จะกลับมาปกติ

ชื่อฟอนต์ต่างกันตามวิธีติดตั้ง:

- **Windows** (winget `DEVCOM.JetBrainsMonoNerdFont`) → `JetBrainsMono NFM`
- **macOS** (brew `--cask font-jetbrains-mono-nerd-font`) → `JetBrainsMono Nerd Font Mono`
- **Linux** (โหลด zip จาก Nerd Fonts release) → `JetBrainsMono Nerd Font Mono`

> **macOS:** ตั้งฟอนต์แล้วต้อง **ปิดโปรแกรม terminal ด้วย Cmd-Q ให้สนิท** เปิดแท็บใหม่ไม่พอ
> เพราะฟอนต์ของ profile ถูกอ่านตอนเปิดแอป — นี่คือสาเหตุอันดับหนึ่งของอาการ
> "ลงฟอนต์แล้วแต่ยังเห็นเป็นกล่อง"
>
> ถ้าใช้ WezTerm ฟอนต์ถูกตั้งไว้ใน `wezterm.lua` ของ dotfiles repo อยู่แล้ว ไม่ต้องตั้งใน UI

> tip: มองหาฟอนต์ที่ลงท้ายด้วย "Nerd Font" หรือ "NF" / "NFM" เสมอ ฟอนต์ธรรมดาที่ชื่อคล้ายกันแต่ไม่มีคำว่า Nerd Font จะไม่มี glyph พวกนี้

---

### 8.6 Clipboard (copy/paste กับข้างนอก)

**อาการ:** `<Space>y` copy แล้วเอาไปวางในเบราว์เซอร์/โปรแกรมอื่นไม่ได้ หรือ `<Space>P` วางของจากข้างนอกไม่มา

**สาเหตุ + รายละเอียด:** `<Space>y` / `<Space>Y` / `<Space>P` ผูกไว้กับ register `+` (system clipboard) config **ไม่ได้บังคับ provider** ตัวไหนเป็นพิเศษ — ปล่อยให้ Neovim เลือกให้อัตโนมัติ (เครื่องนี้มี `win32yank.exe` อยู่แล้ว มันเลยใช้ตัวนั้น)

**วิธีเช็คว่า clipboard ทำงานไหม:**

```vim
:checkhealth vim.provider
:echo has('clipboard_working')    " ได้ 1 = ใช้ได้, ได้ 0 = ยังมีปัญหา
```

ถ้า `checkhealth vim.provider` แจ้งว่าหา clipboard tool ไม่เจอ ก็ลงตัวช่วยอย่าง `win32yank` ให้อยู่บน PATH (แล้วปิด-เปิด terminal ใหม่)

---

### 8.7 checkhealth ขึ้น warning — อันไหนไม่ต้องกังวล

**อาการ:** รัน `:checkhealth` แล้วเห็น **ERROR สีแดง** กับ **WARN สีเหลือง** เต็มไปหมด ตกใจว่า config พังหรือเปล่า

**สาเหตุ + คำอธิบาย:** ใจเย็นครับ — warning/error หลายอันเป็นเรื่องของ tool/provider ที่ **config ชุดนี้ไม่ได้ใช้** เลยไม่กระทบอะไร ไม่ใช่ว่า config พัง อันที่ **ละเว้นได้** มีดังนี้:

| สิ่งที่ checkhealth ฟ้อง | ระดับ | ทำไมไม่ต้องกังวล |
|---|---|---|
| `luarocks` / hererocks | ERROR (แดง) | ไม่มี plugin ตัวไหนใน config นี้ต้องใช้ luarocks — ละเว้นได้เลย |
| provider `node` / `perl` / `python3` / `ruby` | WARN | เป็น remote-plugin provider ที่ plugin ชุดนี้ไม่ได้ใช้ ไม่จำเป็นต้องลง |
| which-key แจ้งว่า `mini.icons` ไม่ได้ลง | WARN | ไม่เป็นไร เพราะ config ใช้ nvim-web-devicons แทนอยู่แล้ว |

สรุปคือของพวกนี้เป็น **optional / ไม่ได้ใช้** ไม่ใช่สัญญาณว่า config เสีย ให้โฟกัสเฉพาะ error ที่เกี่ยวกับ feature ที่คุณใช้จริง (เช่น treesitter, lsp) พอ

---

### 8.8 external tools ที่ต้องมีบน PATH

หลาย feature เด่นๆ ใน config นี้จริงๆ แล้วเป็นแค่ "ตัวห่อ" (wrapper) ที่ไปเรียกโปรแกรมภายนอกอีกที ถ้าโปรแกรมนั้นไม่อยู่บน PATH feature ก็จะใช้ไม่ได้ ตารางนี้สรุปว่าอะไรต้องมี และเช็คยังไง:

| feature | ต้องมี | คำสั่งเช็คใน nvim |
|---|---|---|
| `<Space>ps` (live_grep) | `rg` (ripgrep) | `:echo exepath('rg')` |
| `<Space>gg` (LazyGit) | `lazygit` (plugin เป็นแค่ wrapper) | `:echo exepath('lazygit')` |
| bootstrap / อัปเดต plugin | `git` | `:echo exepath('git')` |
| treesitter | `tree-sitter` + C compiler | (ดูหัวข้อ 8.1) |

ตัวอย่างการลง (อย่าลืมปิด-เปิด terminal หลังลงเสร็จ):

```powershell
# Windows
winget install -e --id BurntSushi.ripgrep.MSVC
winget install -e --id JesseDuffield.lazygit
winget install -e --id Git.Git
```

```bash
# macOS
brew install ripgrep lazygit git
```

```bash
# Linux (Debian/Kali)
sudo apt install ripgrep git
# lazygit ไม่มีใน apt ของ Debian/Kali — โหลดไบนารีจาก release
```

ถ้า `:echo exepath('<tool>')` คืนค่า path มาแปลว่ามันเจอแล้ว ใช้ได้เลย ถ้าคืนสตริงว่างแปลว่ายังไม่อยู่บน PATH

> ใช้ `exepath()` แทน `where.exe` / `which` เพราะมันเช็ค **PATH ที่ nvim เห็นจริง ๆ**
> ไม่ใช่ PATH ของ shell ที่คุณพิมพ์ — สองอันนี้ต่างกันได้ โดยเฉพาะตอนเปิด nvim จาก GUI
> และมันเขียนเหมือนกันหมดทั้งสามแพลตฟอร์ม

---

### 8.9 คำสั่งเช็ค / ซ่อมที่ควรรู้

เก็บตารางนี้ไว้เป็น "ชุดปฐมพยาบาล" เวลามีอะไรแปลกๆ ลองไล่รันดูทีละอันได้เลยครับ

| คำสั่ง | ทำอะไร |
|---|---|
| `:checkhealth` | เช็คสุขภาพทุกอย่างรวดเดียว (จุดเริ่มต้นที่ดีที่สุดเวลามีปัญหา) |
| `:Lazy` | จัดการ plugin — ในเมนูมี `sync` / `update` / `clean` / `restore` |
| `:Lazy profile` | ดูว่า plugin ตัวไหนกินเวลา startup เท่าไร (เอาไว้จูนความเร็ว) |
| `:Mason` | จัดการ LSP server (ลง/ลบ/อัปเดต) — ต้องเปิดไฟล์ก่อน (ดู 8.2) |
| `:MasonLog` | ดู log ตอนติดตั้ง server (เวลา server ลงไม่ผ่าน) |
| `:checkhealth nvim-treesitter` | เช็คว่า treesitter มีเครื่องมือครบไหม + parser ลงตัวไหนแล้ว |
| `:checkhealth vim.lsp` | เช็คสถานะ LSP + ดูว่ามี client active ไหม |

ปิดท้ายด้วยกำลังใจอีกครั้งครับ — ปัญหาส่วนใหญ่ที่เจอตอนเริ่มใช้ Neovim บน Windows แก้ได้ด้วยสองอย่างง่ายๆ คือ "ลงเครื่องมือที่ขาดให้อยู่บน PATH" กับ "ปิด-เปิด terminal ใหม่" เท่านั้นเอง ถ้าติดตรงไหน กลับมาเปิดหมวดนี้ไล่ดูทีละข้อ แล้วคุณจะพาตัวเองผ่านไปได้เองแน่นอน ขอให้สนุกกับ Neovim นะครับ!

---

## 9. ใช้ AI CLI ใน Neovim (Claude Code + codex)

มาถึงหมวดที่หลายคนน่าจะรอคอยครับ — จะใช้ AI coding CLI อย่าง **Claude Code** หรือ **codex** ข้างใน Neovim ยังไงให้ลื่นเหมือนตอนใช้ใน VS Code? ข่าวดีคือ config ตัวนี้เตรียมของไว้ให้แล้ว เราจะไล่ตั้งแต่วิธีที่ง่ายสุดไปจนถึง integration แบบเต็มรูปกันเลย

ก่อนอื่นขอปูภาพให้เห็นตรงกันก่อน ในโลก Neovim เราใช้ AI CLI พวกนี้ได้ **2 ระดับ**:

- **(ก) รันใน terminal เฉยๆ** — เปิด terminal ในตัว Neovim แล้วพิมพ์ `claude` หรือ `codex` ตรงๆ เหมือนเปิด terminal ทั่วไป ได้ chat กับ AI ครบทุกอย่างที่ CLI ทำได้
- **(ข) สำหรับ Claude Code: integration แบบ IDE** — `claudecode.nvim` เชื่อม `claude` กับ Neovim ผ่าน IDE protocol เดียวกับ extension VS Code (ได้ selection + diff ในตัว editor) ส่วน `codex` ใน config นี้รันผ่าน toggleterm แบบธรรมดาเท่านั้น

ต้องพูดตรงๆ ข้อนึงเพื่อไม่ให้เข้าใจผิด: **Anthropic ยังไม่มี extension ทางการสำหรับ Neovim — integration ทางการฝั่ง IDE อยู่ใน VS Code ecosystem (รวม Cursor และ VS Code forks) กับ JetBrains** แต่ชุมชนใจดีทำ integration ที่คุยกับ `claude` ผ่าน **protocol เดียวกัน** ขึ้นมาได้ เลยได้ประสบการณ์ใกล้เคียงกันมากโดยไม่ต้องรอเจ้าของทำเอง — และ config นี้ก็ลงตัวนั้นไว้ให้แล้ว

---

### 9.1 ระดับง่าย: รันใน terminal (มีอยู่แล้ว)

วิธีนี้ไม่ต้องตั้งค่าอะไรเพิ่มเลย ใช้ toggleterm ที่เราเรียนกันไปแล้วในหมวด 6 นี่แหละ:

- เปิด terminal ด้วย `<A-q>` (แบบลอย) หรือ `<A-w>` (แบบแนวนอน) แล้วพิมพ์ `claude` หรือ `codex` ลงไปได้เลย — เทียบง่ายๆ ก็คือ **เทียบเท่ากับการกด Ctrl+J เปิด terminal ใน VS Code** แล้วรัน CLI นั่นเอง
- พออยากออกจากโหมดพิมพ์ terminal กลับมาคุม nvim ตามปกติ กด `<C-\><C-n>` (ชุดเดิมจากหมวด 1 นั่นแหละ)

```
<A-q>            -> เปิด terminal ลอยขึ้นมา
claude           -> พิมพ์แล้ว Enter เพื่อเปิด Claude Code CLI (หรือ codex)
...คุยกับ AI ได้เลย...
<C-\><C-n>       -> ออกจากโหมด terminal กลับมาคุม nvim
```

**ข้อจำกัด:** วิธีนี้ได้แค่ "CLI ล้วนๆ" คือได้ chat กับ AI ครบ แต่ **ไม่มี selection sharing** (ส่งโค้ดที่ select ให้ AI อัตโนมัติ), **ไม่มี inline diff**, และ **ไม่มีปุ่ม accept/reject ในตัว editor** ถ้าอยากได้ของพวกนี้ ต้องขยับไประดับ integration ในหัวข้อถัดไป

---

### 9.2 ระดับ integration: claudecode.nvim (แนะนำสำหรับ Claude)

นี่คือพระเอกของหมวดนี้ครับ config นี้ลง `coder/claudecode.nvim` ไว้ให้แล้ว (plugin spec + keymaps อยู่ใน `lua/plugins/init.lua` ส่วนการตั้งค่า native terminal อยู่ใน `lua/plugins/claudecode.lua`) มันทำงานยังไง?

มันรัน **WebSocket server เล็กๆ** ขึ้นมาในตัว Neovim แล้ว `claude` CLI จะ **เชื่อมเข้าอัตโนมัติ** — เป็น protocol เดียวกับที่ extension ของ VS Code / JetBrains ใช้เลย ผลก็คือคุณจะได้ **selection context + inline diff + accept/reject ของจริงใน nvim** ไม่ต้องคอยก็อป-วางโค้ดไปมาอีกต่อไป

**ตาราง keymap** (ทั้งหมดอยู่ในกลุ่ม `<Space>a` = AI ซึ่ง which-key จะแสดงรายการเมื่อกด `<Space>` ตามด้วย `a` แล้วรอประมาณ 1 วินาที):

| ปุ่ม | ทำอะไร |
|------|--------|
| `<Space>ac` | เปิด/focus Claude Code (toggle) |
| `<Space>ab` | ส่ง buffer ปัจจุบันเป็น context |
| `<Space>as` | (ใน **visual mode**) ส่งโค้ดที่ select ให้ Claude |
| `<Space>aa` | **accept** diff ที่ Claude เสนอ |
| `<Space>ad` | **reject** diff |

**workflow ตัวอย่างแบบจับมือทำ:**

```
1. เปิดไฟล์ที่จะแก้ขึ้นมา
2. <Space>ac              -> เปิด Claude Code ขึ้นมา
3. select โค้ด (visual mode) แล้วกด <Space>as  -> ส่งโค้ดที่เลือกให้ Claude
4. บอก Claude ว่าอยากให้แก้อะไร             -> Claude เสนอ diff กลับมาใน nvim
5. <Space>aa รับ diff / <Space>ad ปฏิเสธ    -> จบงาน!
```

เห็นไหมครับว่าครบวงจร select -> คุย -> เห็น diff -> รับ/ปฏิเสธ อยู่ในหน้าจอ Neovim หน้าเดียว ไม่ต้องสลับไปมาเลย

**ข้อกำหนดก่อนใช้:**

- ต้องมี `claude` CLI อยู่บน **PATH ของ nvim** — เช็คได้โดยพิมพ์ `:echo exepath('claude')` ใน nvim (ใช้ได้เหมือนกันทั้งสามแพลตฟอร์ม และเช็ค PATH ที่ nvim เห็นจริง ๆ) ถ้าคืน path มาแปลว่าเจอแล้ว (ย้ำเรื่องเดิมจากหมวด 8: ต้อง **restart terminal หลังลง claude** ไม่งั้น PATH ยังไม่อัปเดต)
- ต้อง **login / auth** กับ Claude เรียบร้อยแล้ว
- config นี้ตั้งให้ใช้ **native terminal** อยู่แล้ว จึง **ไม่ต้องลง snacks.nvim เพิ่ม** ให้หนักเครื่องเปล่าๆ

> **เกร็ดจาก codex (cross-model):** สองเรื่องที่ควรระวัง — (1) ถ้าคุณใช้ auto-save plugin ให้ตั้งค่าไม่ให้มันบันทึก buffer ที่เป็น diff ของ Claude เพราะบางทีแค่การเซฟ buffer ที่กำลังโชว์ diff อยู่ อาจถูกตีความเป็นการ accept ไปเลย และ (2) ถ้า nvim หา `claude` ไม่เจอ (เช็คด้วย `:echo exepath('claude')` แล้วว่าง) ให้ตั้ง `terminal_cmd` เป็น **path เต็มของตัว `claude`** (บน Windows ตัวที่ใช้ได้คือ shim `claude.cmd` ไม่ใช่ไฟล์ที่ไม่มีนามสกุล) ในไฟล์ `lua/plugins/claudecode.lua` เป็นทางลัดแก้ปัญหา PATH ได้เลย

---

### 9.3 ทางเลือกอื่น (สั้นๆ ไว้ดูอนาคต)

หมวดนี้ขอเล่าไว้พอเป็นแนวทาง เผื่อวันหน้าคุณอยากลองของอื่น ยังไม่ต้องรีบลงตอนนี้ก็ได้ครับ

- **codex CLI** — `codex` CLI รันผ่าน toggleterm ได้ด้วยวิธีเดียวกับ `claude` (ตามหัวข้อ 9.1) แต่ config นี้ยังไม่ได้ติดตั้ง Codex integration เพิ่มเติม หรือถ้าอยากได้ integration ลองดู plugin `folke/sidekick.nvim` ที่คุม **ทั้ง claude + codex ในตัวเดียว** (เขียนโดย folke เจ้าเดียวกับ lazy.nvim / which-key ที่เราใช้อยู่ เลยวางใจเรื่องคุณภาพได้)
- **แบบ Cursor เต็มรูป** — ถ้าอยากได้ UI แบบ chat/sidebar เต็มๆ มี `avante.nvim` และ `codecompanion.nvim` (chat/sidebar + agentic รองรับทั้ง Claude + Codex) พวกนี้ **หนักกว่า** claudecode.nvim พอสมควร เหมาะถ้าอยากได้ประสบการณ์ใกล้เคียง Cursor จริงๆ

ปิดท้ายหมวดนี้ครับ — ตอนนี้คุณมี AI ผู้ช่วยนั่งอยู่ข้างๆ ใน Neovim แล้ว เริ่มจาก 9.1 ให้คุ้นมือก่อน พอพร้อมค่อยขยับไป `<Space>ac` แล้วลองให้ Claude ช่วยแก้โค้ดจริงดูสักรอบ รับรองว่าติดใจ ขอให้สนุกกับการเขียนโค้ดคู่ AI นะครับ!
